## 函数的导数就是函数的变化率．

比如，$y=f(x), f^{\prime}\left(x_{0}\right)=\lim _{\Delta x \rightarrow 0} \frac{f\left(x_{0}+\Delta x\right)-f\left(x_{0}\right)}{\Delta x}=\lim _{\Delta x \rightarrow 0} \frac{\Delta y}{\Delta x}$如图所示

其中，$\Delta y$ 是函数改变量， $\frac{\Delta y}{\Delta x}$ 就是平均改变量。即平均变化率．

$$
\begin{aligned}
& f_{+}^{\prime}\left(x_{0}\right) \\
& =\lim _{\Delta x \rightarrow 0^{+}} \frac{f\left(x_{0}+\Delta x\right)-f\left(x_{0}\right)}{\Delta x}
\end{aligned}
$$

表示在 $x_{0}$ 处沿 $x$ 轴正方向的变化率。

$$
f_{-}^{\prime}\left(x_{0}\right)=\lim _{\Delta x \rightarrow 0^{-}} \frac{f\left(x_{0}+\Delta x\right)-f\left(x_{0}\right)}{\Delta x}
$$

表示在 $x_{0}$ 处沿 $x$ 轴负方向的变化率．

又比如，$z=f(x, y)$ ，偏导数

$$
\begin{aligned}
& f_{x}^{\prime}\left(x_{0}, y_{0}\right)=\lim _{\Delta x \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}\right)-f\left(x_{0}, y_{0}\right)}{\Delta x} \\
& f_{y}^{\prime}\left(x_{0}, y_{0}\right)=\lim _{\Delta y \rightarrow 0} \frac{f\left(x_{0}, y_{0}+\Delta y\right)-f\left(x_{0}, y_{0}\right)}{\Delta y}
\end{aligned}
$$

分别表示函数在点 $\left(x_{0}, y_{0}\right)$ 沿 $x$ 轴方向，沿 $y$ 轴方向的变化率。

如图

特别， $\lim _{\Delta y \rightarrow 0^{+}} \frac{\Delta_{y} z}{\Delta y}=\lim _{\Delta y \rightarrow 0^{+}} \frac{f\left(x_{0}, y_{0}+\Delta y\right)-f\left(x_{0}, y_{0}\right)}{\Delta y}$表示在 $\left(x_{0}, y_{0}\right)$ 处沿 $y$ 轴正方向的变化率。

$$
\text { 而, } \lim _{\Delta y \rightarrow 0^{-}} \frac{\Delta_{y} z}{\Delta y}=\lim _{\Delta y \rightarrow 0^{-}} \frac{f\left(x_{0}, y_{0}+\Delta y\right)-f\left(x_{0}, y_{0}\right)}{\Delta y}
$$

表示在 $\left(x_{0}, y_{0}\right)$ 处沿 $y$ 轴负方向的变化率。

但在许多实际问题中，常需知道 $f(X)$ 在 $X_{0}$沿任何方向的变化率。比如，设 $f(X)$ 表示某物体内部点 $X$ 处的温度。那么，这个物体的热传导就依赖于温度沿各方向下降的速度．

因此有必要引进 $f(X)$ 在 $X_{0}$ 沿一给定方向的方向导数．

把偏导数概念略加推广即可得到方向导数的概念。

即 $f^{6}{ }_{x}\left(x_{0}, y_{0}\right)$ 表示 $y=y_{0}$ 与 $z=f(x, y)$ 的交线在 $M_{0}$ 处的切线对 $x$ 的斜率．

即 $f^{\prime}{ }_{y}\left(x_{0}, y_{0}\right)$ 表示 $x=x_{0}$ 与 $z=f(x, y)$ 的交线在 $M_{0}$ 处的切线对 $y$ 的斜率．

如图

## 2.方向导数定义

定义 设 $z=f(X)=f(x, y)$ 在点 $X_{0}=$
$\left(x_{0}, y_{0}\right)$ 的某邻域 $\mathrm{U}\left(x_{0}\right)$ 内有定义。

以 $X_{0}$ 为端点引射线 $l$ ，其单位方向向量为 $e=(\cos \alpha, \cos \beta)$ ，设 $X =\left(x_{0}+\Delta x, y_{0}+\Delta y\right)$ 是 $l$ 上另一点。

$$
X=\left(x_{0}+\Delta x, y_{0}+\Delta y\right)
$$

若当 $X$ 沿 $l$ 趋于 $X_{0}$ 时，对应的函数改变量与线段 $X_{0} X$ 的长 $\left\|\overrightarrow{X_{0} X}\right\|$ 的比值 $\frac{f(X)-f\left(X_{0}\right)}{\left\|X_{0} X\right\|}$ 的极限存在．

则称它为 $z=f(X)=f(x, y)$ 在点 $X_{0}=\left(x_{0}, y_{0}\right)$沿 $l$ 的方向导数．

$$
\begin{aligned}
& \text { 记作 } \frac{\partial f\left(X_{0}\right)}{\partial l}, \frac{\partial f\left(x_{0}, y_{0}\right)}{\partial l} . \\
& \text { 或 } \frac{\partial f\left(X_{0}\right)}{\partial e}, \frac{\partial f\left(x_{0}, y_{0}\right)}{\partial e} . \\
& \text { 即 } \frac{\partial f\left(x_{0}, y_{0}\right)}{\partial l} \\
& =\lim _{\substack{X \rightarrow X_{0} \\
\text { 沿 } l}} \frac{f(X)-f\left(X_{0}\right)}{\left\|X_{0} X\right\|} \\
& =\lim _{\rho \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}+\Delta y\right)-f\left(x_{0}, y_{0}\right)}{\sqrt{\Delta x^{2}+\Delta y^{2}}} \text {, 其中 } \rho=\sqrt{\Delta x^{2}+\Delta y^{2}} \text {. }
\end{aligned}
$$

注 1．定义中要求点 $X$ 只取在 $l$ 的正向上，且 $X$ 沿 $l$ 趋向于 $X_{0}$ 。另外比值 $\frac{f(X)-f\left(X_{0}\right)}{\left\|X_{0} X\right\|}$的分母大于 0 ．

如图

2．若 $z=f(X)=f(x, y)$ 在 $X_{0}=\left(x_{0}, y_{0}\right)$ 处偏导存在．则在 $X_{0}$ 处沿 $x$ 轴正向的方向导数，（此时，$\Delta y=0, \Delta x>0$ ），

$$
\begin{aligned}
\frac{\partial f\left(x_{0}, y_{0}\right)}{\partial l} & =\lim _{\rho \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}\right)-f\left(x_{0}, y_{0}\right)}{\sqrt{\Delta x^{2}+0^{2}}} \\
& =\lim _{|\Delta x| \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}\right)-f\left(x_{0}, y_{0}\right)}{|\Delta x|} \\
& =\lim _{\Delta x \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}\right)-f\left(x_{0}, y_{0}\right)}{\Delta x}=f_{x}^{\prime}\left(x_{0}, y_{0}\right)
\end{aligned}
$$

在 $X_{0}$ 处沿 $x$ 轴负方向的方向导数，（此时，$\Delta y=0, \Delta x<0$ ），

$$
\begin{aligned}
\frac{\partial f\left(x_{0}, y_{0}\right)}{\partial l} & =\lim _{\rho \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}\right)-f\left(x_{0}, y_{0}\right)}{\sqrt{\Delta x^{2}}} \\
& =\lim _{|\Delta x| \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}\right)-f\left(x_{0}, y_{0}\right)}{|\Delta x|} \\
& =\lim _{\Delta x \rightarrow 0} \frac{f\left(x_{0}+\Delta x, y_{0}\right)-f\left(x_{0}, y_{0}\right)}{-\Delta x}=-f_{x}^{\prime}\left(x_{0}, y_{0}\right)
\end{aligned}
$$

同样可得沿 $y$ 轴正向的方向导数为 $f_{y}^{\prime}\left(x_{0}, y_{0}\right)$ ，而沿 $y$ 轴负方向的方向导数为 $-f^{\prime}{ }_{y}\left(x_{0}, y_{0}\right)$ 。

3．定义中的极限表示式可用另一形式给出。由于 $l$ 的单位方向向量为 $e=(\cos \alpha, \cos \beta)$ ，从而 $l$ 的参数式方程为 $\left\{\begin{array}{l}x=x_{0}+t \cos \alpha \\ y=y_{0}+t \cos \beta\end{array} \quad t>0\right.$或 $(x, y)=\left(x_{0}, y_{0}\right)+t(\cos \alpha, \cos \beta)$ ，即 $X=X_{0}+t e$且 $\left\|\overrightarrow{X_{0} X}\right\|=\left\|X-X_{0}\right\|=\|t e\|=t$而 $X \rightarrow X_{0}$ 就是 $t \rightarrow 0^{+}$．

从而

$$
\begin{aligned}
\frac{\partial f\left(X_{0}\right)}{\partial l} & =\lim _{\substack{X \rightarrow X_{0} \\
\text { 沿 } l}} \frac{f(X)-f\left(X_{0}\right)}{\left\|X_{0} X\right\|} \\
& =\lim _{t \rightarrow 0^{+}} \frac{f(X+t e)-f\left(X_{0}\right)}{t}
\end{aligned}
$$

这正是教材中给出的定义式．

## 3.方向导数的存在定理

定理 若 $z=f(X)=f(x, y)$ 在点 $X_{0}=\left(x_{0}, y_{0}\right)$ 可微，则 $z=f(X)$ 在 $X_{0}$ 沿任一方向 $e=(\cos \alpha, \cos \beta)$的方向导数存在。 $e$ 为单位向量。

$$
\text { 且 } \begin{aligned}
& \frac{\partial f\left(X_{0}\right)}{\partial e}=\frac{\partial f\left(X_{0}\right)}{\partial x} \cos \alpha+\frac{\partial f\left(X_{0}\right)}{\partial y} \cos \beta \\
= & \left(\frac{\partial f\left(X_{0}\right)}{\partial x}, \frac{\partial f\left(X_{0}\right)}{\partial y}\right) \cdot(\cos \alpha, \cos \beta) \\
= & \boldsymbol{J}_{f}\left(\boldsymbol{X}_{0}\right) \cdot \boldsymbol{e} . \text { (最后两式为数量积) }
\end{aligned}
$$

证：如图
在射线 $l$ 上取点

$$
\begin{aligned}
X & =\left(x_{0}+\Delta x, y_{0}+\Delta y\right) \\
& =X_{0}+\Delta X
\end{aligned}
$$

其中，$\Delta X=(\Delta x, \Delta y)$
因向量 $\Delta X=X-X_{0}=\overrightarrow{X_{0} X} / / e$ ，
故 $\Delta X=t e,(t>0)$ ， $X=X_{0}+t e$ ，

$$
\left\|\overrightarrow{X_{0} X}\right\|=\|\Delta X\|=t
$$

## 由方向导数定义

$$
\begin{aligned}
\frac{\partial f\left(X_{0}\right)}{\partial e} & =\lim _{X \rightarrow X_{0}} \frac{f(X)-f\left(X_{0}\right)}{\left\|X_{0} X\right\|} \\
& =\lim _{t \rightarrow 0^{+}} \frac{f\left(X_{0}+t e\right)-f\left(X_{0}\right)}{t}
\end{aligned}
$$

看 $f\left(X_{0}+t e\right)-f\left(X_{0}\right)$ ．

因 $f(X)$ 在 $X_{0}$ 可微，知

$$
\begin{aligned}
\Delta z & =f\left(X_{0}+\Delta X\right)-f\left(X_{0}\right) \\
& =f\left(x_{0}+\Delta x, y_{0}+\Delta y\right)-f\left(x_{0}, y_{0}\right) \\
& =a \Delta x+b \Delta y+0\left(\sqrt{\Delta x^{2}+\Delta y^{2}}\right) \\
& =\frac{\partial f\left(X_{0}\right)}{\partial x} \Delta x+\frac{\partial f\left(X_{0}\right)}{\partial y} \Delta y+0(\|\Delta X\|) \\
& =J_{f}\left(X_{0}\right) \cdot \Delta X+0(\|\Delta X\|)
\end{aligned}
$$

即 $\Delta z=f\left(X_{0}+\Delta X\right)-f\left(X_{0}\right)=J_{f}\left(X_{0}\right) \cdot \Delta X+0(\|\Delta X\|)$
上式对任何 $\Delta x, \Delta y$ 都成立。
特别，当 $X=X_{0}+\Delta X$ 在射线 $l$ 上时，当然成立．
即，当 $X_{0}+\Delta X=X_{0}+t e$ 时，有

$$
\begin{aligned}
f\left(X_{0}+t e\right)-f\left(X_{0}\right) & =J_{f}\left(X_{0}\right) \cdot(t e)+0(\|t e\|) \\
& =t\left[\left(J_{f}\left(X_{0}\right) \cdot e\right]+0(t)\right.
\end{aligned}
$$

除以 $t>0$ ，并令 $t \rightarrow 0^{+}$，有

$$
\begin{aligned}
\frac{\partial f\left(X_{0}\right)}{\partial e} & =\lim _{t \rightarrow 0^{+}} \frac{f\left(X_{0}+t e\right)-f\left(X_{0}\right)}{t} \\
& =\lim _{t \rightarrow 0^{+}}\left[J_{f}\left(X_{0}\right) \cdot e+\frac{0(t)}{t}\right]=J_{f}\left(X_{0}\right) \cdot e \\
& =\frac{\partial f\left(X_{0}\right)}{\partial x} \cos \alpha+\frac{\partial f\left(X_{0}\right)}{\partial y} \cos \beta
\end{aligned}
$$

特别：

- 当 $\boldsymbol{l}$ 与 $\boldsymbol{x}$ 轴同向 $\left(\alpha=0, \beta=\frac{\pi}{2}\right)$ 时，有 $\frac{\partial f}{\partial l}=\frac{\partial f}{\partial x}$
- 当 $\boldsymbol{l}$ 与 $\boldsymbol{x}$ 轴反向 $\left(\alpha=\pi, \beta=\frac{\pi}{2}\right)$ 时，有 $\frac{\partial f}{\partial l}=-\frac{\partial f}{\partial x}$

4．推广
公式可推广到三元函数中去．
即，若 $u=f(x, y, z)$ 在点 $X_{0}=\left(x_{0}, y_{0}, z_{0}\right)$ 可微，则 $u$ 在该点处沿任何方向 $e=(\cos \alpha, \cos \beta, \cos \gamma)$的方向导数存在

且 $\frac{\partial f\left(X_{0}\right)}{\partial e}=J_{f}\left(X_{0}\right) \cdot e$

$$
=\frac{\partial f\left(X_{0}\right)}{\partial x} \cos \alpha+\frac{\partial f\left(X_{0}\right)}{\partial y} \cos \beta+\frac{\partial f\left(X_{0}\right)}{\partial z} \cos \gamma
$$

## 5.方向导数的计算习例

例1 求 $z=x^{2}+y^{2}$ 在点 $(1,2)$ 处沿从 $(1,2)$ 到 $(2,2+\sqrt{3})$的方向的方向导数。

例2 求 $z=1-\left(\frac{x^{2}}{a^{2}}+\frac{y^{2}}{b^{2}}\right)$ 在点 $\left(\frac{a}{\sqrt{2}}, \frac{b}{\sqrt{2}}\right)$ 处沿曲线 $\frac{x^{2}}{a^{2}}+\frac{y^{2}}{b^{2}}=1$在这点的内法线方向的方向导数。

例3 设由 $(0,0)$ 到 $(x, y)$ 的向径为 $\vec{r}, x$ 轴到 $\vec{r}$ 的转角为 $\theta$ ， $x$ 轴到射线 $l$ 的转角为 $\varphi$ ，求 $\frac{\partial r}{\partial l}$ ，其中 $r=|\vec{r}|=\sqrt{x^{2}+y^{2}}$ ．

例4 设 $\vec{n}$ 是曲面 $2 x^{2}+3 y^{2}+z^{2}=6$ 在点 $P(1,1,1)$ 处指向外侧的法向量，求函数 $u=\frac{\sqrt{6 x^{2}+8 y^{2}}}{z}$ 在点 $P$ 处沿方向 $\vec{n}$ 的方向导数。

例1 求 $z=x^{2}+y^{2}$ 在点 $(1,2)$ 处沿从 $(1,2)$ 到 $(2,2+\sqrt{3})$的方向的方向导数。

解

$$
\begin{gathered}
\because z_{x}(1,2)=\left.2 x\right|_{x=1}=2, \quad z_{y}(1,2)=\left.2 y\right|_{y=2}=4 \\
\text { 方向 } l=\{1, \sqrt{3}\} \\
\cos \alpha=\frac{1}{\sqrt{1+3}}=\frac{1}{2}, \cos \beta=\frac{\sqrt{3}}{\sqrt{1+3}}=\frac{\sqrt{3}}{2} \\
\left.\therefore \frac{\partial z}{\partial l}\right|_{(1,2)}=z_{x}(1,2) \cos \alpha+z_{y}(1,2) \cos \beta=1+2 \sqrt{3} .
\end{gathered}
$$

例2 求 $z=1-\left(\frac{x^{2}}{a^{2}}+\frac{y^{2}}{b^{2}}\right)$ 在点 $\left(\frac{a}{\sqrt{2}}, \frac{b}{\sqrt{2}}\right)$ 处沿曲线 $\frac{x^{2}}{a^{2}}+\frac{y^{2}}{b^{2}}=1$在这点的内法线方向的方向导数。

解1 如图所示 $\because \frac{x^{2}}{a^{2}}+\frac{y^{2}}{b^{2}}=1$ 的切线倾角满足
∴ 在 $\left(\frac{\boldsymbol{a}}{\sqrt{\mathbf{2}}}, \frac{\boldsymbol{b}}{\sqrt{\mathbf{2}}}\right)$ 处 $\tan \alpha=-\frac{\boldsymbol{b}}{\boldsymbol{a}} \quad\left(\frac{\pi}{\mathbf{2}}<\alpha<\pi\right)$

$$
\begin{aligned}
& \text { 又 } \varphi=-\left[\frac{\pi}{2}+(\pi-\alpha)\right]=\alpha-\frac{3}{2} \pi \\
& \cos \varphi=-\sin \alpha=-\frac{b}{\sqrt{a^{2}+b^{2}}}, \sin \varphi=\cos \alpha=-\frac{a}{\sqrt{a^{2}+b^{2}}} \\
& \text { 而 } \frac{\partial z}{\partial x}=-\frac{2 x}{a^{2}}, \quad \frac{\partial z}{\partial y}=-\frac{2 y}{b^{2}} \\
& \left.\therefore \frac{\partial z}{\partial x}\right|_{\left(\frac{a}{\sqrt{2}}, \frac{b}{\sqrt{2}}\right)}=-\frac{\sqrt{2}}{a},\left.\quad \frac{\partial z}{\partial y}\right|_{\left(\frac{a}{\sqrt{2}}, \frac{b}{\sqrt{2}}\right)}=-\frac{\sqrt{2}}{b} \\
& \left.\therefore \frac{\partial f}{\partial l}\right|_{\left(\frac{a}{\sqrt{2}}, \frac{b}{\sqrt{2}}\right)}=\frac{\partial z}{\partial x} \cos \varphi+\frac{\partial z}{\partial y} \sin \varphi=\frac{1}{a b} \sqrt{2\left(a^{2}+b^{2}\right)} .
\end{aligned}
$$

解2 曲线 $\frac{x^{2}}{a^{2}}+\frac{y^{2}}{b^{2}}=1$ 的法向量为 $\left\{\frac{2 x}{a^{2}}, \frac{2 y}{b^{2}}\right\}$
在点 $\left(\frac{\boldsymbol{a}}{\sqrt{2}}, \frac{\boldsymbol{b}}{\sqrt{2}}\right)$ 处法向量为 $\left\{\frac{\sqrt{2}}{\boldsymbol{a}}, \frac{\sqrt{2}}{\boldsymbol{b}}\right\}$
从而内法向量可取为 $\vec{n}=-\left\{\frac{1}{a}, \frac{1}{b}\right\}=\left\{-\frac{1}{a},-\frac{1}{b}\right\}$

$$
\begin{aligned}
& \cos \alpha=\frac{-\frac{1}{a}}{\sqrt{\left(-\frac{1}{a}\right)^{2}+\left(-\frac{1}{b}\right)^{2}}}=-\frac{b}{\sqrt{a^{2}+b^{2}}}, \cos \beta=-\frac{a}{\sqrt{a^{2}+b^{2}}} \\
& \left.\therefore \frac{\partial f}{\partial l}\right|_{\left(\frac{a}{\sqrt{2}}, \frac{b}{\sqrt{2}}\right)}=\frac{\partial z}{\partial x} \cos \alpha+\frac{\partial z}{\partial y} \cos \beta=\frac{1}{a b} \sqrt{2\left(a^{2}+b^{2}\right)} .
\end{aligned}
$$

例3 设由 $(0,0)$ 到 $(x, y)$ 的向径为 $\vec{r}, x$ 轴到 $\vec{r}$ 的转角为 $\theta$ ， $x$ 轴到射线 $l$ 的转角为 $\varphi$ ，求 $\frac{\partial r}{\partial l}$ ，其中 $r=|\vec{r}|=\sqrt{x^{2}+y^{2}}$ ．解 如图所示

$$
\begin{aligned}
\because \frac{\partial r}{\partial x} & =\frac{2 x}{2 \sqrt{x^{2}+y^{2}}}=\frac{x}{r}=\cos \theta \\
& \frac{\partial r}{\partial y}=\frac{y}{r}=\sin \theta
\end{aligned}
$$

$\therefore \frac{\partial r}{\partial l}=\cos \theta \cos \varphi+\sin \theta \sin \varphi=\cos (\theta-\varphi)$
且当 $\varphi=\theta$ 时，$\frac{\partial r}{\partial l}=1$ ；当 $\varphi=\theta \pm \frac{\pi}{2}$ 时，$\frac{\partial r}{\partial l}=0$ ．

例4 设 $\vec{n}$ 是曲面 $2 x^{2}+3 y^{2}+z^{2}=6$ 在点 $P(1,1,1)$ 处指向外侧的法向量，求函数 $u=\frac{\sqrt{6 x^{2}+8 y^{2}}}{z}$ 在点 $P$ 处沿方向 $\vec{n}$ 的方向导数。

解 $\quad \vec{n}=\left.(4 x, 6 y, 2 z)\right|_{P}=2(2,3,1)$
方向余弦为 $\cos \alpha=\frac{2}{\sqrt{14}}, \cos \beta=\frac{3}{\sqrt{14}}, \cos \gamma=\frac{1}{\sqrt{14}}$
而 $\left.\quad \frac{\partial u}{\partial x}\right|_{P}=\left.\frac{6 x}{z \sqrt{6 x^{2}+8 y^{2}}}\right|_{P}=\frac{6}{\sqrt{14}}$
同理得 $\left.\frac{\partial u}{\partial y}\right|_{P}=\frac{8}{\sqrt{14}},\left.\quad \frac{\partial u}{\partial z}\right|_{P}=-\sqrt{14}$

$$
\left.\therefore \quad \frac{\partial u}{\partial n}\right|_{P}=\frac{1}{14}(6 \times 2+8 \times 3-14 \times 1)=\frac{11}{7}
$$
