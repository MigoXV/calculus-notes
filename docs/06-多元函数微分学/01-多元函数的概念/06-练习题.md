## 一、填空：

1．已知 $f(x, y)=\frac{x y}{x^{2}+y}$ ，则 $f\left(x y, \frac{x}{y}\right)=$
2．设 $z=\sqrt{y}+f(\sqrt{x}-1)$ ，且当 $y=1$ 时，$z=x$ ，则 $f(y)=$ $\_\_\_\_$ ＂

3．设 $f(x, y)=a x+b y$ ，其中 $a, b$ 为常数，则 $f[x y, f(x, y)]=$ $\_\_\_\_$
4．函数 $z=\frac{\sqrt{4 x-y^{2}}}{\ln \left(1-x^{2}-y^{2}\right)}$ 的定义域为
5．函数 $f(x, y)=\frac{\arcsin \left(3-x^{2}-y^{2}\right)}{\sqrt{y^{2}-3 x}}$ 的定义域为

## 二、选择题

1．$z=\ln \sqrt{x^{2}-y^{2}}$ 的定义域为（ ）．
（A）$\quad x^{2}-y^{2} \geq 1$
（B）$x^{2}-y^{2} \geq 0$
（C）$x^{2}-y^{2}>1$
（D）$x^{2}-y^{2}>0$

2．函数 $z=\ln \left(x^{2}+y^{2}-1\right)+\sqrt{2-x^{2}-y^{2}}$ 的定义域为（）．
（A）$x^{2}+y^{2}>1$
（B） $1<x^{2}+y^{2} \leq 2$
（B）$x^{2}+y^{2} \leq 2$
（D）$x^{2}+y^{2} \geq 2$

三、求下列函数的定义域，并画出定义域的图形
1．$z=\frac{1}{\sqrt{x+y}}-\frac{1}{\sqrt{x-y}}$
2．$z=\ln (y-x)+\frac{\sqrt{x}}{\sqrt{1-x^{2}-y^{2}}}+$

解：（1）

## 一、判断题

1．如果 $P$ 沿任何直线 $y=k x$ 趋于 $(0,0)$ ，都有 $\lim _{\substack{x \rightarrow 0 \\ y=6 x}} f(P)=A$ ，则 $\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} f(x, y)=A$ ．

2．从 $\lim _{x \rightarrow 0} f(x, 0)=0$ 和 $\lim _{x \rightarrow 0} f(x, 2 x)=2$ 知 $\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} f(x, y)$ 不存在．

## 二、填空题：

1． $\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} \frac{\ln \left(x+e^{y}\right)}{x+y}=\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} \frac{x y\left(x^{2}-y^{2}\right)}{x^{2}+y^{2}}=$
2．$z=\ln \left(2-x^{2}-y^{2}\right)$ 的不连续点是圆 $\_\_\_\_$上的点

3．$z=\sin \frac{1}{x \nu}$ 的不连续点是 $\_\_\_\_$ ，

三、求下列极限
1． $\lim _{\substack{x \rightarrow \infty \\ y \rightarrow \infty}} \frac{x^{2}+y^{2}}{x^{4}+y^{4}}$
2． $\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} \frac{1-\cos \sqrt{x^{2}+y^{2}}}{x^{2}+y^{2}}$
3． $\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} \frac{\sqrt{x y+1}-1}{x y}$

解：三、1．（法一）
（法二）做坐标变换，令 $x=r \cos \theta, y=r \sin \theta$ ，则

2．做坐标变换，令 $x=r \cos \theta, y=r \sin \theta$ ，则
$\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} \frac{1-\cos \sqrt{x^{2}+y^{2}}}{x^{2}+y^{2}}=$
3． $\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} \frac{\sqrt{x y+1}-1}{x y}$

四、证明 $\lim _{\substack{x \rightarrow 0 \\ y \rightarrow 0}} \frac{x^{2} y^{2}}{x^{2} y^{2}+(x-y)^{2}}$ 不存在．

五、讨论 $f(x, y)=\left\{\begin{array}{ll}\frac{x^{5}+y^{5}}{x^{2}+y^{2}}, & (x, y) \neq(0,0) \\ 0, & (x, y)=(0,0)\end{array}\right.$ 在 $(0,0)$ 处的连续性．
