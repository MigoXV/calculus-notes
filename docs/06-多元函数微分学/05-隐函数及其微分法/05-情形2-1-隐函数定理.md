## 情形2－－1 $F, G \in C^{1}$, 方程组 定理：设 $\left\{\begin{array}{l}F(x, y, z)=0 \\ G(x, y, z)=0\end{array}\right.$

（1）$F(x, y, z), G(x, y, z)$ 在含点 $\left(x_{0}, y_{0}, z_{0}\right)$ 的一个开区域 $D$ 上连续；
（2）$F, G$ 在 $D$ 上有连续的偏导数；
（3）$F\left(x_{0}, y_{0}, z_{0}\right)=G\left(x_{0}, y_{0}, z_{0}\right)=0$ ；
（4）$\frac{\partial(F, G)}{\partial(y, z)}=\left|\begin{array}{ll}F_{y} & F_{z} \\ G_{y} & G_{z}\end{array}\right| \neq 0$ ；
（4）式为雅可比行列式
则（1）存在 $\delta>0$ 和定义 在
$\Delta=\left(x_{0}-\delta, x_{0}+\delta\right)$ 上惟一的函数 $\left\{\begin{array}{l}y=y(x), \\ z=z(x),\end{array}\right.$
使得 $\left\{\begin{array}{l}F(x, y(x), z(x))=0 \\ G(x, y(x), z(x))=0\end{array}, x \in \Delta\right.$

定理中隐函数存在的证明从略，仅就求导公式推导如下：

利用隐函数求导的方法，
对方程组 $\left\{\begin{array}{l}F(x, y(x), z(x))=0 \\ G(x, y(x), z(x))=0\end{array}\right\} \frac{d z}{d x}=-\frac{\left|\begin{array}{ll}F_{y} & F_{x} \\ G_{y} & G_{x}\end{array}\right|}{\left|\begin{array}{ll}F_{y} & F_{z} \\ G_{y} & G_{z}\end{array}\right|}=-\frac{\frac{\partial(F, G)}{\partial(y, x)}}{\frac{\partial(F, G)}{\partial(y, z)}}$对 $x$ 求导，得

$$
\left\{\begin{array} { l } 
{ F _ { x } + F _ { y } \cdot y ^ { \prime } + F _ { z } \cdot z ^ { \prime } = 0 } \\
{ G _ { x } + G _ { y } \cdot y ^ { \prime } + G _ { z } \cdot z ^ { \prime } = 0 }
\end{array} \Leftrightarrow \left\{\begin{array}{l}
F_{y} \cdot y^{\prime}+F_{z} \cdot=-\frac{J_{y x}}{J_{y z}} \\
G_{y} \cdot y^{\prime}+G_{z} \cdot z=-\mathrm{U}_{x}
\end{array}\right.\right.
$$

若 $\left|\begin{array}{ll}F_{y} & F_{z} \\ G_{y} & G_{z}\end{array}\right| \neq 0$（即 $J_{y z}=\frac{\partial(F, G)}{\partial(y, z)}$ ），由克莱姆法则，知

$$
\frac{d y}{d x}=-\frac{\left|\begin{array}{ll}
F_{x} & F_{z} \\
G_{x} & G_{z}
\end{array}\right|}{\left|\begin{array}{ll}
F_{y} & F_{z} \\
G_{y} & G_{z}
\end{array}\right|}=-\frac{\frac{\partial(F, G)}{\partial(x, z)}}{\frac{\partial(F, G)}{\partial(y, z)}}=-\frac{J_{x z}}{J_{y z}}
$$

