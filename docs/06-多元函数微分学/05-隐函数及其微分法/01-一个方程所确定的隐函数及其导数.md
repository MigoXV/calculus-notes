## 一、一个方程所确定的隐函数及其导数

定理1．设函数 $F(x, y)$ 在点 $P\left(x_{0}, y_{0}\right)$ 的某一邻域内满足
（1）具有连续的偏导数；
（2）$F\left(x_{0}, y_{0}\right)=0$ ；
（3）$F_{y}\left(x_{0}, y_{0}\right) \neq 0$
则方程 $F(x, y)=0$ 在点 $x_{0}$ 的某邻域内可唯一确定一个单值连续函数 $y=f(x)$ ，满足条件 $y_{0}=f\left(x_{0}\right)$ ，并有连续导数

$$
\frac{\mathrm{d} y}{\mathrm{~d} x}=-\frac{F_{x}}{F_{y}} \quad \text { (隐函数求导公式) }
$$

定理证明从略，仅就求导公式推导如下：

设 $y=f(x)$ 为方程 $F(x, y)=0$ 所确定的隐函数，则

$$
F(x, f(x)) \equiv 0
$$

