## 二、向量积

1．引例。设 $O$ 为杜杆 $L$ 的支点，有一个与杜杆夹角为 $\theta$的力 $\vec{F}$ 作用在杜杆的 $P$ 点上，则力 $\vec{F}$ 作用在杜杆上的力矩是一个向量 $\vec{M}$ ：
$|\vec{M}|=|O Q\|\vec{F}|=|\overrightarrow{O P} \| \vec{F}| \sin \theta \overrightarrow{O P} \Rightarrow \vec{F} \Rightarrow \vec{M}$ 符合右手规则
$\vec{M} \perp \overrightarrow{O P} \vec{M} \perp \vec{F}$

$$
O Q|=|\overrightarrow{O P}| \sin \theta
$$

## 2.向量积的定义

设 $\vec{a}, \vec{b}$ 的夹角为 $\theta$ ，定义
向量 $\vec{c}\left\{\begin{array}{l}\text { 方向 }: \vec{c} \perp \vec{a}, \vec{c} \perp \vec{b} \text { 且符合右手规则 } \\ \text { 模 ：}|\vec{c}|=|\vec{a}||\vec{b}| \sin \theta\end{array}\right.$
称 $\vec{c}$ 为向量 $\vec{a}$ 与 $\vec{b}$ 的向量积，记作
$\vec{c}=\vec{a} \times \vec{b}$（叉积）
引例中的力矩 $\vec{M}=\overrightarrow{O P} \times \vec{F}$

思考：右图三角形面积

$$
S=\frac{1}{2}|\vec{a} \times \vec{b}|
$$

## 3.向量积的性质

（1）$\vec{a} \times \vec{a}=\overrightarrow{0} . \quad(\because \theta=0 \Rightarrow \sin \theta=0)$
（2）$\vec{a} / / \vec{b} \Longleftrightarrow \vec{a} \times \vec{b}=\overrightarrow{0} . \quad(\vec{a} \neq \overrightarrow{0}, \vec{b} \neq \overrightarrow{0})$

证 $(\Rightarrow) \because \vec{a} \times \vec{b}=\overrightarrow{0}, \quad|\vec{a}| \neq 0, \quad|\vec{b}| \neq 0$ ，

$$
\therefore \sin \theta=0, \quad \theta=0, \quad \vec{a} / / \vec{b}
$$

（ ⟸）$\because \vec{a} / / \vec{b} \quad \therefore \theta=0$ 或 $\pi \quad \therefore \sin \theta=0$
$|\vec{a} \times \vec{b}|=|\vec{a} \| \vec{b}| \sin \theta=\mathbf{0}$.
特别地： $\overrightarrow{\boldsymbol{i}} \times \overrightarrow{\boldsymbol{i}}=\overrightarrow{\boldsymbol{j}} \times \overrightarrow{\boldsymbol{j}}=\overrightarrow{\boldsymbol{k}} \times \overrightarrow{\boldsymbol{k}}=\overrightarrow{\mathbf{0}}$ ；

$$
\vec{i} \times \vec{j}=\vec{k}, \vec{j} \times \vec{k}=\vec{i}, \vec{k} \times \vec{i}=\vec{j}
$$

向量积满足下列运算规律：
（1）$\vec{a} \times \vec{b}=-\vec{b} \times \vec{a}$ ．
（2）分配律：$(\vec{a}+\vec{b}) \times \vec{c}=\vec{a} \times \vec{c}+\vec{b} \times \vec{c}$ ．
（3）若 $\lambda$ 为数：$(\lambda \vec{a}) \times \vec{b}=\vec{a} \times(\lambda \vec{b})=\lambda(\vec{a} \times \vec{b})$ ．

4．向量积的坐标表示
设 $\vec{a}=a_{x} \vec{i}+a_{y} \vec{j}+a_{z} \vec{k}, \quad \vec{b}=b_{x} \vec{i}+b_{y} \vec{j}+b_{z} \vec{k}$
$\vec{a} \times \vec{b}=\left(a_{x} \vec{i}+a_{y} \vec{j}+a_{z} \vec{k}\right) \times\left(b_{x} \vec{i}+b_{y} \vec{j}+b_{z} \vec{k}\right)$
$\because \vec{i} \times \vec{i}=\vec{j} \times \vec{j}=\vec{k} \times \vec{k}=\overrightarrow{0}$,
$\because \vec{i} \times \vec{j}=\vec{k}, \quad \vec{j} \times \vec{k}=\vec{i}, \quad \vec{k} \times \vec{i}=\vec{j}$,
$\vec{j} \times \vec{i}=-\vec{k}, \quad \vec{k} \times \vec{j}=-\vec{i}, \quad \vec{i} \times \vec{k}=-\vec{j}$.
$=\left(a_{y} b_{z}-a_{z} b_{y}\right) \vec{i}+\left(a_{z} b_{x}-a_{x} b_{z}\right) \vec{j}+\left(a_{x} b_{y}-a_{y} b_{x}\right) \vec{k}$
向量积的坐标表达式

向量积还可用三阶行列式表示

$$
\vec{a} \times \vec{b}=\left|\begin{array}{ccc}
\vec{i} & \vec{j} & \vec{k} \\
a_{x} & a_{y} & a_{z} \\
b_{x} & b_{y} & b_{z}
\end{array}\right|=\vec{i}\left|\begin{array}{cc}
a_{y} & a_{z} \\
b_{y} & b_{z}
\end{array}\right|-\vec{j}\left|\begin{array}{cc}
a_{x} & a_{z} \\
b_{x} & b_{z}
\end{array}\right|+\vec{k}\left|\begin{array}{cc}
a_{x} & a_{y} \\
b_{x} & b_{y}
\end{array}\right|
$$

由上式可推出： $\overrightarrow{\boldsymbol{a}} / / \overrightarrow{\boldsymbol{b}} \Longleftrightarrow \frac{\boldsymbol{a}_{x}}{\boldsymbol{b}_{x}}=\frac{\boldsymbol{a}_{y}}{\boldsymbol{b}_{y}}=\frac{\boldsymbol{a}_{z}}{\boldsymbol{b}_{z}} \boldsymbol{b}_{\boldsymbol{x}} 、 \boldsymbol{b}_{\boldsymbol{y}} 、 \boldsymbol{b}_{\boldsymbol{z}}$ 不能同时为零，但允许两个为零，例如，$\frac{\boldsymbol{a}_{x}}{\mathbf{0}}=\frac{\boldsymbol{a}_{y}}{\mathbf{0}}=\frac{\boldsymbol{a}_{z}}{\boldsymbol{b}_{z}} \Rightarrow \boldsymbol{a}_{x}=\mathbf{0}, \boldsymbol{a}_{y}=\mathbf{0}$

## 补充

$|\overrightarrow{\boldsymbol{a}} \times \overrightarrow{\boldsymbol{b}}|$ 表示以 $\overrightarrow{\boldsymbol{a}}$ 和 $\overrightarrow{\boldsymbol{b}}$ 为邻边的平行四边形的面积．

## 向量积习例

例6 求与 $\vec{a}=3 \vec{i}-2 \vec{j}+4 \vec{k}, \vec{b}=\vec{i}+\vec{j}-2 \vec{k}$ 都垂直的单位向量．

例 7 在顶点为 $A(1,-1,2) 、 B(5,-6,2)$ 和 $C(1,3,-1)$ 的三角形中，求 $A C$ 边上的高 $B D$ ．

例8 设向量 $\overrightarrow{\boldsymbol{m}}, \overrightarrow{\boldsymbol{n}}, \overrightarrow{\boldsymbol{p}}$ 两两垂直，符合右手规则，且 $|\vec{m}|=4,|\vec{n}|=2,|\vec{p}|=3$ ，计算 $(\vec{m} \times \vec{n}) \cdot \vec{p}$ 。

例6 求与 $\overrightarrow{\boldsymbol{a}}=\mathbf{3} \overrightarrow{\boldsymbol{i}}-\mathbf{2} \overrightarrow{\boldsymbol{j}}+\mathbf{4} \overrightarrow{\boldsymbol{k}}, \overrightarrow{\boldsymbol{b}}=\overrightarrow{\boldsymbol{i}}+\overrightarrow{\boldsymbol{j}}-\mathbf{2} \overrightarrow{\boldsymbol{k}}$ 都垂直的单位向量．

$$
\begin{aligned}
& \text { 解 } \\
& \left.\vec{c}=\vec{a} \times \vec{b}=\left|\begin{array}{ccc}
\vec{i} & \vec{j} & \vec{k} \\
a_{x} & a_{y} & a_{z} \\
b_{x} & b_{y} & b_{z}
\end{array}\right|=\begin{array}{ccc}
\vec{i} & \vec{j} & \vec{k} \\
3 & -2 & 4 \\
1 & 1 & -2
\end{array} \right\rvert\,=10 \vec{j}+5 \vec{k}, \\
& \because|\vec{c}|=\sqrt{10^{2}+5^{2}}=5 \sqrt{5}, \\
& \therefore \overrightarrow{c^{0}}= \pm \frac{\vec{c}}{|\vec{c}|}= \pm\left(\frac{2}{\sqrt{5}} \vec{j}+\frac{1}{\sqrt{5}} \vec{k}\right) .
\end{aligned}
$$

例 7 在顶点为 $\boldsymbol{A}(\mathbf{1},-\mathbf{1}, \mathbf{2}) 、 \boldsymbol{B}(\mathbf{5},-\mathbf{6 , 2})$ 和 $C(1,3,-1)$ 的三角形中，求 $A C$ 边上的高 $B D$ 。

解 $\overrightarrow{A C}=\{0,4,-3\}$

$$
\overrightarrow{A B}=\{4,-5,0\}
$$

三角形 $A B C$ 的面积为

$$
\begin{aligned}
& S=\frac{1}{2}|\overrightarrow{A C} \times \overrightarrow{A B}|=\frac{1}{2} \sqrt{15^{2}+12^{2}+16^{2}}=\frac{25}{2}, \\
& |\overrightarrow{A C}|=\sqrt{4^{2}+(-3)^{2}}=5, \quad S=\frac{1}{2}|\overrightarrow{A C}| \cdot|B D| \\
& \frac{25}{2}=\frac{1}{2} \cdot 5 \cdot|B D| \quad \therefore|B D|=5
\end{aligned}
$$

例8 设向量 $\overrightarrow{\boldsymbol{m}}, \overrightarrow{\boldsymbol{n}}, \overrightarrow{\boldsymbol{p}}$ 两两垂直，符合右手规则，且 $|\vec{m}|=4,|\vec{n}|=2,|\vec{p}|=3$ ，计算 $(\vec{m} \times \vec{n}) \cdot \vec{p}$ 。

解 $\quad|\vec{m} \times \vec{n}|=|\vec{m} \| \vec{n}| \sin (\vec{m}, \vec{n})$

$$
=4 \times 2 \times 1=8 \text {, }
$$

依题意知 $\overrightarrow{\boldsymbol{m}} \times \overrightarrow{\boldsymbol{n}}$ 与 $\overrightarrow{\boldsymbol{p}}$ 同向，

$$
\begin{aligned}
& \therefore \theta=(\vec{m} \times \vec{n}, \hat{p})=0 \\
& (\vec{m} \times \vec{n}) \cdot \vec{p}=|\vec{m} \times \vec{n}| \cdot|\vec{p}| \cos \theta=8 \cdot 3=24
\end{aligned}
$$
