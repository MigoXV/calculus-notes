## 一、向量的数量积

1．引 例：如图所示，人在路面上用绳子拉一个物体，绳子上的力 $F$ 与路面成的角为 $\theta$ ，物体产生的位移为 $S$ ，则对物体作的功为

$$
W=|F| \cdot|S| \cos \theta
$$

注意：两向量作这样的运算，结果是一个数量．

2．两向量的数量积定义
定义 向量 $\vec{a}$ 与 $\vec{b}$ 的数量积为 $\vec{a} \cdot \vec{b}$

$$
\vec{a} \cdot \vec{b}=|\vec{a}||\vec{b}| \cos \theta
$$

（其中 $\theta$ 为 $\vec{a}$ 与 $\vec{b}$ 的夹角）
注意：向量 $\vec{a}$ 与 $\vec{b}$ 的数量积表示为 $\vec{a} \cdot \vec{b}$ 。
如果 $\vec{a}=\overrightarrow{0}$ 与 $\vec{b}=\overrightarrow{0}$ ，则 $\vec{a} \cdot \vec{b}=0$ 。

3．数量积的投影表示

$$
\vec{a} \cdot \vec{b}=|\vec{a}||\vec{b}| \cos \theta
$$

$$
\begin{aligned}
& \because|\vec{b}| \cos \theta=\operatorname{Pr} j_{a} \vec{b}, \quad|\vec{a}| \cos \theta=\operatorname{Pr} j_{b} \vec{a}, \\
& \therefore \vec{a} \cdot \vec{b}=|\vec{b}| \operatorname{Pr} j_{b} \vec{a}=|\vec{a}| \operatorname{Pr} j_{a} \vec{b} .
\end{aligned}
$$

结论 两向量的数量积等于其中一个向量的模和另一个向量在这向量的方向上的投影的乘积。

数量积也称为＂点积＂、＂内积＂。

## 关于数量积的说明：

（1）$\vec{a} \cdot \vec{a}=|\vec{a}|^{2}$ ．
证 $\because \theta=0, \quad \therefore \vec{a} \cdot \vec{a}=|\vec{a}||\vec{a}| \cos \theta=|\vec{a}|^{2}$ ．
（2）$\vec{a} \cdot \vec{b}=\mathbf{0} \Longleftrightarrow \vec{a} \perp \vec{b}$ ．
证（ $\Rightarrow) \because \vec{a} \cdot \vec{b}=\mathbf{0},|\vec{a}| \neq \mathbf{0},|\vec{b}| \neq \mathbf{0}$ ，

$$
\text { ( } \begin{aligned}
& \therefore \cos \theta=0, \quad \theta=\frac{\pi}{2}, \quad \therefore \vec{a} \perp \vec{b} . \\
& \because \vec{a} \perp \vec{b}, \quad \therefore \theta=\frac{\pi}{2}, \quad \therefore \cos \theta=0, \\
& \vec{a} \cdot \vec{b}=|\vec{a}||\vec{b}| \cos \theta=0 .
\end{aligned}
$$

## 4.向量数量积的基本性质

特别地：

$$
\begin{aligned}
& \vec{i} \cdot \vec{i}=\vec{j} \cdot \vec{j} \rightrightarrows \vec{k} \cdot \vec{k}=1 ; \\
& \vec{i} \cdot \vec{j}=\vec{j} \cdot \vec{k} \supseteq \vec{i} \cdot \vec{k}=0
\end{aligned}
$$

数量积符合下列运算规律：
（1）交换律：$\vec{a} \cdot \vec{b}=\vec{b} \cdot \vec{a}$ ；
（2）分配律：$(\vec{a}+\vec{b}) \cdot \vec{c}=\vec{a} \cdot \vec{c}+\vec{b} \cdot \vec{c}$ ；
（3）若 $\lambda$ 为数：$(\lambda \vec{a}) \cdot \vec{b}=\vec{a} \cdot(\lambda \vec{b})=\lambda(\vec{a} \cdot \vec{b})$ ，若 $\lambda, \mu$ 为数：$(\lambda \vec{a}) \cdot(\mu \vec{b})=\lambda \mu(\vec{a} \cdot \vec{b})$ ．

5．向量数量积的坐标表示及应用
设 $\vec{a}=a_{x} \vec{i}+a_{y} \vec{j}+a_{z} \vec{k}, \quad \vec{b}=b_{x} \vec{i}+b_{y} \vec{j}+b_{z} \vec{k}$

$$
\left.\begin{array}{l}
\vec{a} \cdot \vec{b}=\left(a_{x} \vec{i}+a_{y} \vec{j}+a_{z} \vec{k}\right) \cdot\left(b_{x} \vec{i}+b_{y} \vec{j}+b_{z} \vec{k}\right) \\
\quad \because \vec{i} \perp \vec{j} \perp \vec{k}, \quad \therefore \vec{i} \cdot \vec{j}=\vec{j} \cdot \vec{k}=\vec{k} \cdot \vec{i}=0, \\
\quad \because|\vec{i}|=|\vec{j}|=|\vec{k}|=1, \\
\quad \therefore \vec{i} \cdot \vec{i}=\vec{j} \cdot \vec{j}=\vec{k} \cdot \vec{k}=1 .
\end{array}\right]
$$

$$
\vec{a} \cdot \vec{b}=|\vec{a} \| \vec{b}| \cos \theta \Rightarrow \cos \theta=\frac{\vec{a} \cdot \vec{b}}{|\vec{a}||\vec{b}|},
$$

（1） $\cos \theta=\frac{a_{x} b_{x}+a_{y} b_{y}+a_{z} b_{z}}{\sqrt{a_{x}{ }^{2}+a_{y}{ }^{2}+a_{z}{ }^{2}} \sqrt{b_{x}{ }^{2}+b_{y}{ }^{2}+b_{z}{ }^{2}}}$

## 两向量夹角余弦的坐标表示式

由此可知两向量垂直的充要条件为
（2）$\vec{a} \perp \vec{b} \Longleftrightarrow a_{x} b_{x}+a_{y} b_{y}+a_{z} b_{z}=0$
（3） $\operatorname{Pr} j_{\vec{b}} \vec{a}=\frac{\vec{a} \cdot \vec{b}}{|\vec{b}|}$ 或 $\operatorname{Pr} j_{\vec{a}} \vec{b}=\frac{\vec{a} \cdot \vec{b}}{|\vec{a}|}$

例 1 已知 $\vec{a}=\{\mathbf{1}, \mathbf{1},-\mathbf{4}\}, \vec{b}=\{\mathbf{1},-\mathbf{2}, \mathbf{2}\}$ ，求（1）$\vec{a} \cdot \vec{b}$ ； （2）$\vec{a}$ 与 $\vec{b}$ 的夹角；（3）$\vec{a}$ 在 $\vec{b}$ 上的投影．

例2 证明向量 $\vec{c}$ 与向量 $(\vec{a} \cdot \vec{c}) \vec{b}-(\vec{b} \cdot \vec{c}) \vec{a}$ 垂直．
例3 设 $\vec{\alpha}, \vec{\beta}$ 为任意向量，证明：$|\vec{\alpha}+\vec{\beta}| \leq|\vec{\alpha}|+|\vec{\beta}|$ ．
例4 设 $\vec{a}, \vec{b}, \vec{c}$ 为单位向量，且 $\vec{a}+\vec{b}+\vec{c}=\overrightarrow{0}$ ，求 $\vec{a} \cdot \vec{b}+\vec{b} \cdot \vec{c}+\vec{c} \cdot \vec{a}$ ．
例 5 已知向量 $\vec{a}, \vec{b}$ 的夹角 $\theta=\frac{3 \pi}{4}$ ，且 $|\vec{a}|=\sqrt{2},|\vec{b}|=3$ ，求 $|\vec{a}-\vec{b}|$ ．

例 1 已知 $\overrightarrow{\boldsymbol{a}}=\{\mathbf{1}, \mathbf{1},-\mathbf{4}\}, \overrightarrow{\boldsymbol{b}}=\{\mathbf{1},-\mathbf{2}, \mathbf{2}\}$ ，求 $(\mathbf{1}) \overrightarrow{\boldsymbol{a}} \cdot \overrightarrow{\boldsymbol{b}}$ ；
（2）$\vec{a}$ 与 $\vec{b}$ 的夹角；（3）$\vec{a}$ 在 $\vec{b}$ 上的投影．
解（1）$\vec{a} \cdot \vec{b}=1 \cdot 1+1 \cdot(-2)+(-4) \cdot 2=-9$ ．
（2） $\cos \theta=\frac{a_{x} b_{x}+a_{y} b_{y}+a_{z} b_{z}}{\sqrt{a_{x}^{2}+a_{y}^{2}+a_{z}^{2}} \sqrt{b_{x}^{2}+b_{y}^{2}+b_{z}^{2}}}$

$$
=-\frac{1}{\sqrt{2}}, \quad \therefore \theta=\frac{3 \pi}{4} .
$$

（3）$\vec{a} \cdot \vec{b}=|\vec{b}| \operatorname{Pr} j_{b} \vec{a} \quad \therefore \operatorname{Pr} j_{b} \vec{a}=\frac{\vec{a} \cdot \vec{b}}{|\vec{b}|}=-3$ ．

例2 证明向量 $\vec{c}$ 与向量 $(\vec{a} \cdot \vec{c}) \vec{b}-(\vec{b} \cdot \vec{c}) \vec{a}$ 垂直．

证 $\quad[(\vec{a} \cdot \vec{c}) \vec{b}-(\vec{b} \cdot \vec{c}) \vec{a}] \cdot \vec{c}$

$$
\begin{aligned}
& =[(\vec{a} \cdot \vec{c}) \vec{b} \cdot \vec{c}-(\vec{b} \cdot \vec{c}) \vec{a} \cdot \vec{c}] \\
& =(\vec{c} \cdot \vec{b})[\vec{a} \cdot \vec{c}-\vec{a} \cdot \vec{c}] \\
& =0 \\
& \therefore[(\vec{a} \cdot \vec{c}) \vec{b}-(\vec{b} \cdot \vec{c}) \vec{a}] \perp \vec{c}
\end{aligned}
$$

例3 设 $\vec{\alpha}, \vec{\beta}$ 为任意向量，证明：$|\vec{\alpha}+\vec{\beta}| \leq|\vec{\alpha}|+|\vec{\beta}|$ ．
证 $\quad \because|\vec{\alpha}+\vec{\beta}|^{2}=(\vec{\alpha}+\vec{\beta}) \cdot(\vec{\alpha}+\vec{\beta})$

$$
=|\vec{\alpha}|^{2}+2 \vec{\alpha} \cdot \vec{\beta}+|\vec{\beta}|^{2}
$$

而 $\vec{\alpha} \cdot \vec{\beta}=|\vec{\alpha}||\vec{\beta}| \cos (\vec{\alpha}, \vec{\beta}) \leq|\vec{\alpha}||\vec{\beta}|$

$$
\therefore|\vec{\alpha}+\vec{\beta}|^{2} \leq|\vec{\alpha}|^{2}+2|\vec{\alpha}||\vec{\beta}|+|\vec{\beta}|^{2}=(|\vec{\alpha}|+|\vec{\beta}|)^{2}
$$

即 $|\vec{\alpha}+\vec{\beta}| \leq|\vec{\alpha}|+|\vec{\beta}|$ 。

例4 设 $\vec{a}, \vec{b}, \vec{c}$ 为单位向量，且 $\vec{a}+\vec{b}+\vec{c}=\overrightarrow{0}$ ，求 $\vec{a} \cdot \vec{b}+\vec{b} \cdot \vec{c}+\vec{c} \cdot \vec{a}$ ．

解 已知 $|\overrightarrow{\boldsymbol{a}}|=|\overrightarrow{\boldsymbol{b}}|=|\overrightarrow{\boldsymbol{c}}|=\mathbf{1}$ ，且 $\overrightarrow{\boldsymbol{a}}+\overrightarrow{\boldsymbol{b}}+\overrightarrow{\boldsymbol{c}}=\overrightarrow{\mathbf{0}}$ ，故 $(\vec{a}+\vec{b}+\vec{c}) \cdot(\vec{a}+\vec{b}+\vec{c})=0$ ，即 $|\vec{a}|^{2}+|\vec{b}|^{2}+|\vec{c}|^{2}+2 \vec{a} \cdot \vec{b}+2 \vec{b} \cdot \vec{c}+2 \vec{c} \cdot \vec{a}=0$, $\therefore \vec{a} \cdot \vec{b}+\vec{b} \cdot \vec{c}+\vec{c} \cdot \vec{a}=-\frac{1}{2}\left(|\vec{a}|^{2}+|\vec{b}|^{2}+|\vec{c}|^{2}\right)=-\frac{3}{2}$ ．

例 5 已知向量 $\vec{a}, \vec{b}$ 的夹角 $\theta=\frac{3 \pi}{4}$ ，且 $|\vec{a}|=\sqrt{2},|\vec{b}|=3$ ，求 $|\vec{a}-\vec{b}|$ 。

解 $\because|\vec{a}-\vec{b}|^{2}=(\vec{a}-\vec{b}) \cdot(\vec{a}-\vec{b})$

$$
\begin{aligned}
& =|\vec{a}|^{2}-2 \vec{a} \cdot \vec{b}-|\vec{b}|^{2} \\
& =2-6 \sqrt{2} \cdot\left(-\frac{\sqrt{2}}{2}\right)+9=17 \\
\therefore|\vec{a}-\vec{b}| & =\sqrt{17} .
\end{aligned}
$$
