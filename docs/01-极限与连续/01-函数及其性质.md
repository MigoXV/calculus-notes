
第1章 函数与极限

1.1 函数及其性质

1.1.1 集合的概念 1.1.2 集合的运算 1.1.3 区间与邻域 1.1.4 函数的映射 1.1.5 函数的概念 1.1.6 函数的特性 1.1.7 反函数与复合函数 1.1.8 函数的四则运算 1.1.9 初等函数

## 1.1 函数及其性质

## 1.1.1 集合

1．集合的概念
2．集合的表示法 3．数集分类： 1.1.2 集合的基本运算

$$
A-B=\{x \mid x \in A \text { 且 } x \notin B\}=A \bar{B}
$$

对偶律：$\overline{\mathrm{A} \cup \mathrm{B}}=\overline{\mathrm{A}} \cap \overline{\mathrm{B}}$，$\overline{\mathrm{A} \cap \mathrm{B}}=\overline{\mathrm{A}} \cup \overline{\mathrm{B}}$

$$
\overline{\bigcup_{i=1}^{\infty} A_{i}}=\bigcap_{i=1}^{\infty} \overline{A_{i}}, \quad \overline{\bigcap_{i=1}^{\infty} A_{i}}=\bigcup_{i=1}^{\infty} \overline{A_{i}}
$$

设 $A, B$ 是两个非空集合，且 $x \in A, y \in B$ ，则称有次序的一对元素 $x, y$ 为一个序偶，记作 $(x, y)$ 。由集合 $A, B$ 中所有元素作成的序偶构成的集合，称为 $A$ 与 $B$ 的直积或Cartesian乘积，记作 $A \times B$ ，即

$$
A \times B=\{(x, y) \mid x \in A, y \in B\}
$$

例如，$R \times R=\{(x, y) \mid x \in R, y \in R\}$ 表示整个坐标平面，记作 $R^{2}$ ，即 $R^{2}=R \times R=\{(x, y) \mid x \in R, y \in R\}$ 。

练习：$A=\{-1,2\}, B=\{1,2,3\}$ ，求 $A \times B$ 。

## 1.1.3 区间与邻域

区间：是指介于某两个实数之间的全体实数．这两个实数叫做区间的端点。
$\forall a, b \in R$ ，且 $a<b$ ．
$\{\boldsymbol{x} \mid \boldsymbol{a}<\boldsymbol{x}<\boldsymbol{b}\}$ 称为开区间，记作 $(\boldsymbol{a}, \boldsymbol{b})$
$\{x \mid a \leq x \leq b\}$ 称为闭区间，记作 $[a, b]$
$\{\boldsymbol{x} \mid \boldsymbol{a} \leq \boldsymbol{x}<\boldsymbol{b}\} \quad$ 称为半开区间，记作 $[\boldsymbol{a}, \boldsymbol{b})$
$\{x \mid a<x \leq b\} \quad$ 称为半开区间，记作 $(a, b]$
有限区间

$$
[a,+\infty)=\{x \mid a \leq x\} \quad(-\infty, b)=\{x \mid x<b\}
$$

无限区间

区间长度的定义：
两端点间的距离（线段的长度）称为区间的长度．

设 $x_{0}$ 与 $\delta$ 是两个实数，且 $\delta>0$ ．
数集 $\left\{x \mid \left|x-x_{0}\right|<\delta\right\}$ 称为点 $x_{0}$ 的 $\delta$ 邻域，点 $x_{0}$ 叫做这邻域的中心， $\delta$ 叫做这邻域的半径。

$$
U\left(x_{0}, \delta\right)=\left\{x \mid x_{0}-\delta<x<x_{0}+\delta\right\} .
$$

点 $x_{0}$ 的去心的 $\delta$ 邻域，记作 $U\left(\bar{x}_{0}, \delta\right)$

$$
U\left(\bar{x}_{0}, \delta\right)=\left\{x\left|0<\left|x-x_{0}\right|<\delta\right\} .\right.
$$

邻域引入的作用：根据 $\delta(\delta>0)$ 的变化，刻画变量 $x$ 逼近常量 $x_{0}$（即 $x \rightarrow x_{0}$ ）的程度。

## 1.1.5 函数的概念

## 1.函数的定义

设 $\boldsymbol{D}$ 是实数集，称映射 $f: D \rightarrow \mathbf{R}$ 为定义在 $\boldsymbol{D}$ 上的函数，通常简记为

因变量，自变量

数集 $\boldsymbol{D}$ 叫做函数 $\boldsymbol{f}$ 的定义域，记作 $D(f)$ ，即 $D(f)=D$
函数值 $f(x)$ 的全体所构成的数集称为函数 $f$ 的值域，
记作 $R(f)$ 或 $f(D)$ ，即 $R(f)=f(D)=\{y \mid y=f(x), x \in D\}$
可见，函数是从实数集到实数集的映射，其值域总在 $\mathbf{R}$内，因此构成函数的要素是：定义域与对应法则

故若两个函数的定义域相同，对应法则也相同，则这两个函数就是相同的，否则就是不同的。

判断函数 $f$ 与 $g$ 是否是同一函数？
（1）$f(x)=\lg x^{2}, g(x)=2 \lg x$
（2） $\boldsymbol{f}(\boldsymbol{x})=\boldsymbol{x}, \boldsymbol{g}(\boldsymbol{x})=\sqrt{\boldsymbol{x}^{2}}$
（3）$f(x)=\sqrt[3]{x^{4}-x^{3}}, g(x)=x \sqrt[3]{x-1}$
表示函数的记号是可以任意选取的，除了常用的 $f$ 外，还可以用其他的英文字母或希腊字母，比如 $g \quad \varphi$＂等，相应的函数可记为

$$
y=g(x), y=F(x), y=\varphi(x)
$$

单值函数与多值函数
如果自变量在定义域内任取一个数值时，对应的函数值总是只有一个，这种函数叫做单值函数；否则叫做多值函数。

例如，$x^{2}+y^{2}=a^{2}$ ．
只有一个自变量的函数，称为一元函数．

## 2.函数定义域的确定

（1）由实际问题决定。
（2）自然定义域。理论研究中，对应法则是用数学公式表示的函数，这种函数的定义域是使数学公式有意义的自变量的所有值构成的实数集．即当函数由公式（表达式）给出时，使公式有意义的自变量的取值范围就是函数的定义域。如：

分式的分母不为 $\mathbf{0}$;

$\sqrt[2 n]{f(x)}, n$ 为正整数, 要求 $f(x) \geq 0$;

$\log _{a} f(x)$, 要求 $a>0$ 且 $a \neq 1, f(x)>0$;

$\arcsin f(x), \arccos f(x)$, 要求 $|f(x)| \leq 1$;

$y=f(x)^{g(x)}$, 要求 $f(x)>0$.

（3）定义域的表示法：
不等式法，集合法，区间法，叙述法与图示法．

## 例 求函数的定义域（1） $\boldsymbol{y}=\sqrt{4-\boldsymbol{x}^{2}}+\frac{1}{\sqrt{\boldsymbol{x}-1}}$ <br> 解： <br> （1）要求 $\left\{\begin{array}{c}4-x^{2} \geq 0 \\ x-1>0\end{array} \Rightarrow \mathbf{1}<\boldsymbol{x} \leq \mathbf{2}\right.$ <br> （2）$f(x)=\frac{1}{1+\frac{1}{1+\frac{1}{x}}}$ ．

所以函数的定义域为 $(\mathbf{1 , 2 ]}$ ．
（2）要求 $\left\{\begin{array}{l}x \neq 0 \\ 1+\frac{1}{x} \neq 0 \\ 1+\frac{1}{1+\frac{1}{x}} \neq 0\end{array} \Rightarrow \boldsymbol{x} \neq \mathbf{0}, \boldsymbol{- 1}, \boldsymbol{-} \frac{\mathbf{1}}{\mathbf{2}}\right.$ ．

3．函数的图形
点集 $C=\{(x, y) \mid y=f(x), x \in D\}$
称为函数 $y=f(x)$ 的图形．

## 4.分段函数

对于自变量的不同值（或在不同区间上），函数的表达式不同，这种函数称为分段函数。
（1）绝对值函数

$$
y=|x|= \begin{cases}x, & x \geq 0 \\ -x, & x<0\end{cases}
$$

（2）符号函数

$$
y=\operatorname{sgn} x=\left\{\begin{array}{cc}
1, & x>0 \\
0, & x=0 \\
-1, & x<0
\end{array}\right.
$$

（3）取整函数 $y=[x]$
$[x]$ 表示不超过 $x$ 的最大整数
（4）Dirichlet（狄利克雷）函数
$y=D(x)= \begin{cases}1 & \text { 当 } x \text { 是有理数时 } \\ 0 & \text { 当 } x \text { 是无理数时 }\end{cases}$
（5）取最值函数

$$
y=\max \{f(x), g(x)\} \quad y=\min \{f(x), g(x)\}
$$

（6）整标函数
以自然数为自变量的函数：$\quad y=f(n)$
图形为一些离散的点构成。

## 1.1.6 函数的特性

1．函数的单调性
设函数 $y=f(x)$ 在区间 $I$（有限或无限，开或闭）上有定义，对任意 $x_{1}, x_{2} \in I$ ，若 $x_{1}<x_{2}$ ，有

$$
f\left(x_{1}\right)<f\left(x_{2}\right) \quad\left(\text { 或 } f\left(x_{1}\right)>f\left(x_{2}\right)\right)
$$

则称 $f(x)$ 在 $I$ 上严格单调上升或严格单调递增（严格单调下降或严格单调递减）。

对任意 $x_{1}, x_{2} \in I$ ，若 $x_{1}<x_{2}$ ，有

$$
f\left(x_{1}\right) \leq f\left(x_{2}\right) \quad\left(\text { 或 } f\left(x_{1}\right) \geq f\left(x_{2}\right)\right)
$$

则称 $f(x)$ 在 $I$ 上单调上升或单调递增（单调下降或单调递减）。单增和单减的函数统称为单调函数，$I$ 称为单调区间。

由有限个单调函数组成的函数，称为分段单调函数。如 $y=x$

## 2.函数的有界性

若 $X \subset D, \exists M>0, \forall x \in X$ ，有 $|f(x)| \leq M$ 成立，则称函数 $f(x)$ 在 $X$ 上有界。否则称无界。

即若 $X \subset D, \forall M>0, \exists x_{1} \in X$ ，有 $\left|f\left(x_{1}\right)\right|>M$ 成立，则称函数 $f(x)$ 在 $X$ 上无界。
通常函数的有界性与区间有关，如 $y=\frac{1}{x^{2}}$ ，在 $(0,1)$ 内无界，而在 $\left[\frac{1}{10}, 1\right)$ 上有界。

## 3.函数的奇偶性

设 $D$ 关于原点对称，对于 $\forall x \in D$ ，有 $\boldsymbol{f}(-\boldsymbol{x})=\boldsymbol{f}(\boldsymbol{x})$ 称 $\boldsymbol{f}(\boldsymbol{x})$ 为偶函数；

偶函数图形关于 $y$ 轴对称

奇函数图形关于原点对称

设 $D$ 关于原点对称，对于 $\forall x \in D$ ，有
$f(-x)=-f(x) \quad$ 称 $f(x)$ 为奇函数．

注意：（1）若 $f(x)$ 的定义域关于原点不对称，则 $f(x)$ 一定不是奇函数或偶函数．
（2）对于 $(-a, a)(a>0)$ 上的任意函数 $f(x)$ ，则
$g(x)=f(x)+f(-x)$ 为偶函数 ，
$h(x)=f(x)-f(-x)$ 为奇函数，
从而 $f(x)=\frac{1}{2}[g(x)+h(x)]$ ，
即 $f(x)$ 可表示为一个偶函数与一个奇函数之和。
（3）奇偶函数的性质
偶函数的和与差仍是偶函数，
奇函数的和与差仍是奇函数；
两个奇（或偶）函数的商是偶函数；
奇函数与偶函数的积（或商）是奇函数；
有限个偶函数的积仍是偶函数；
偶数个奇函数的积是偶函数．

## 4.函数的周期性

设函数 $y=f(x)$ 在 $D$ 上有定义，若 $\exists T \neq 0, \forall x \in D$ ，都有 $f(x+T)=f(x)$ ，则称 $f(x)$ 为周期函数，$T$ 为周期。

任一周期函数都有无穷多个周期．若在无穷多个周期中，存在一个最小的正数，则这个正数称为最小正周期，简称周期．
并非所有周期函数都有最小正周期，如Dirichlet函数

$$
y=D(x)=\left\{\begin{array}{c}
1, x \in \mathbf{Q} \\
0, x \in \mathbf{Q}^{C}
\end{array}\right.
$$

容易验证这是一个周期函数，任何正有理数都是它的周期，因为不存在最小的正有理数，所以Dirichlet函数没有最小正周期．

## 1.1.7 反函数与复合函数

## 1.反函数

定义：设函数 $f: D \rightarrow f(D)$ 是单射，则它存在逆映射 $f^{-1}: f(D) \rightarrow D$ ，称此映射 $f^{-1}$ 为函数 $f$ 的反函数．
亦即 设 $y=f(x)$ 在 $X$ 上有定义，值域 $f(X)=Y$ 。
若对任一个 $y \in Y$ 都有唯一确定的 $x \in X$ ，使得 $f(x)=y$ ，

$$
x=f^{-1}(y), y \in Y .
$$

注意：（1）反函数的定义域和值域恰好是原来函数的值域和定义域．
（2）直接函数与反函数的图形关于 $y=x$ 对称．
（3）反函数的对应法则是完全由原函数的对应法则所确定。

## 反函数的求法：

（1）一般先从方程 $y=f(x)$ 中解出 $x$ ，然后再将所得结果中的 $x$ 与 $y$ 互换位置即可；
（2）对分段函数，只要分段求出反函数便得．

## 2.复合函数

定义：设有两个函数 $y=f(u)(u \in U)$ 与 $u=g(x)(x \in X)$ ，且函数 $u=g(x)$ 的值域 $g(X) \subset U$ ，

则在 $X$ 上确定了函数 $y=f[g(x)], x \in X$ ，
称为 $y=f(u)$ 与 $u=g(x)$ 的复合函数。记为 $y=f \circ g$
注意：（1）复合函数关键是 $: g(X) \subset U$
即不是任何两个函数都可以复合成一个复合函数的．
（2）复合函数可以由两个以上的函数经过复合构成。
（3）一般 $f \circ g \neq g \circ f$
复合函数的求法：
（1）对于非分段函数常用直接代入的方法；
（2）对于分段函数常用讨论的方法．

练习 1．由函数 $y=\sqrt{u} \quad u \in[0,+\infty)$ ，

$$
u=1-x^{2} \quad x \in(-\infty,+\infty)
$$

可构成复合函数 $\quad y=\sqrt{1-x^{2}} \quad x \in[-1,1]$

## 函数复合后一般应重新验证它的定义域

2．分解 $y=\arccos \sqrt{\ln \left(x^{2}-1\right)}$

$$
y=\arccos u, u=\sqrt{v}, v=\ln w, w=\left(x^{2}-1\right),
$$

分解到基本初等函数或基本初等函数的四则运算为止．

形如：$y=u(x)^{v(x)}$ 的函数称为幂指函数。
3．$y=(1+\sin x)^{\frac{1}{x}}$

$$
y=(1+\sin x)^{\frac{1}{x}}=e^{\ln (1+\sin x)^{\frac{1}{x}}}=e^{\frac{1}{x} \ln (1+\sin x)}
$$

即 $y=(1+\sin x)^{\frac{1}{x}}$ 是由 $y=e^{u}$ ，

$$
\begin{aligned}
& u=\frac{1}{x} \ln v, \\
& v=1+\sin x
\end{aligned}
$$

## 1.1.8 函数的四则运算

## 函数的四则运算

设函数 $f(x), g(x)$ 的定义域分别为 $D_{1}, D_{2}, D=D_{1} \cap D_{2} \neq \varnothing$ ，则可以定义这两个函数的下列运算：

和 $\quad f+g:(f+g)(x)=f(x)+g(x), x \in D$ ；
差 $\quad f-g:(f-g)(x)=f(x)-g(x), x \in D$ ；
积 $\quad f \cdot g:(f \cdot g)(x)=f(x) \cdot g(x), x \in D$ ；
商 $\frac{f}{g}:\left(\frac{f}{g}\right)(x)=\frac{f(x)}{g(x)}, x \in D-\{x \mid g(x)=0, x \in D\}$.
这四种运算称为函数的四则运算．

## 1.1.9 初等函数

1．基本初等函数
（1）常数函数
$y=c, x \in(-\infty,+\infty), c$ 为常数．
（2）幂函数
$y=x^{\mu} \quad(\mu$ 是常数 $)$
（3）指数函数

$$
y=a^{x} \quad(a>0, a \neq 1) \quad y=e^{x}
$$

（4）对数函数

$$
y=\log _{a} x \quad(a>0, a \neq 1) \quad y=\ln x
$$

## （5）三角函数

正弦函数 $y=\sin x$

余弦函数 $\boldsymbol{y}=\cos \boldsymbol{x}$

正切函数 $y=\tan x$

余切函数 $\boldsymbol{y}=\cot \boldsymbol{x}$

正割函数 $y=\sec x=\frac{1}{\cos x}$

余割函数 $y=\csc x=\frac{1}{\sin x}$

## （6）反三角函数

反正弦函数 $y=\arcsin x$

反余弦函数 $\boldsymbol{y}=\arccos \boldsymbol{x}$

反正切函数 $y=\arctan x$

反余切函数 $y=\operatorname{arccot} x$

## 基本初等函数

幂函数，指数函数，对数函数，三角函数和反三角函数统称为基本初等函数。

## 2.初等函数

由常数和基本初等函数经过有限次四则运算和有限次的函数复合步骤所构成并可用二个式子表示的函数，称为初等函数。

并非所有的函数都是初等函数，分段函数一般不是初等函数。但也有例外！

幂指函数 $y=x^{\frac{1}{x}}, x>0$ 是否为初等函数？
解 $y=x^{\frac{1}{x}}=e^{\frac{1}{x} \ln x}$
它是由 $y=e^{u}$ 与 $u=\frac{\ln x}{x}$ 构成的复合函数，故该幂指函数是一个初等函数．

3．双曲函数与反双曲函数也是初等函数．

## 附：双曲函数与反双曲函数

## （1）双曲函数

双曲正弦 $\operatorname{sh} x=\frac{e^{x}-e^{-x}}{2} \boldsymbol{D}$ ：$(-\infty,+\infty)$ ，奇函数．

双曲余弦 $\operatorname{ch} x=\frac{e^{x}+e^{-x}}{2} \boldsymbol{D}:(-\infty,+\infty)$ ，偶函数．

双曲正切 $\operatorname{th} x=\frac{\operatorname{sh} x}{\operatorname{ch} x}=\frac{e^{x}-e^{-x}}{e^{x}+e^{-x}}$
$D:(-\infty,+\infty) \quad$ 奇函数，有界函数，

双曲函数常用公式

$$
\begin{aligned}
& \operatorname{sh}(x \pm y)=\operatorname{sh} x \operatorname{ch} y \pm \operatorname{ch} x \operatorname{sh} y ; \\
& \operatorname{ch}(x \pm y)=\operatorname{ch} x \operatorname{ch} y \pm \operatorname{sh} x \operatorname{sh} y ; \\
& \operatorname{ch}^{2} x-\operatorname{sh}^{2} x=1 ; \\
& \operatorname{sh} 2 x=2 \operatorname{sh} x \operatorname{ch} x ; \\
& \operatorname{ch} 2 x=\operatorname{ch}^{2} x+\operatorname{sh}^{2} x .
\end{aligned}
$$

## $(2)$ 反双曲函数

反双曲正弦 $y=a r \operatorname{sh} x$ ；

$$
\begin{aligned}
y & =\operatorname{arsh} x \\
& =\ln \left(x+\sqrt{x^{2}+1}\right) .
\end{aligned}
$$

$$
D:(-\infty,+\infty)
$$

奇函数，

在（ $-\infty,+\infty$ ）内单调增加。

例 求函数 $f(x)=\frac{1}{2}\left(e^{x}-e^{-x}\right)$ 的反函数
解：令 $y=\frac{1}{2}\left(e^{x}-e^{-x}\right)$
则 $e^{2 x}-2 y e^{x}-1=0$

$$
\begin{aligned}
& e^{x}=y \pm \sqrt{y^{2}+1} \quad(\text { 舍去"-") } \\
& x=\ln \left(y+\sqrt{y^{2}+1}\right)
\end{aligned}
$$

将字母 $x$ 与 $y$ 互换，得 $y=\ln \left(x+\sqrt{x^{2}+1}\right)$
即 $f^{-1}(x)=\ln \left(x+\sqrt{x^{2}+1}\right)$

反双曲余弦 $\boldsymbol{y}=\operatorname{arch} x$

$$
\begin{aligned}
y & =\operatorname{arch} x \\
& =\ln \left(x+\sqrt{x^{2}-1}\right)
\end{aligned}
$$

$$
D:[1,+\infty)
$$

在 $[1,+\infty)$ 内单调增加．

反双曲正切 $\boldsymbol{y}=a r t h x$

$$
\begin{aligned}
y & =\operatorname{arth} x \\
& =\frac{\mathbf{1}}{\mathbf{2}} \ln \frac{\mathbf{1}+\boldsymbol{x}}{\mathbf{1}-\boldsymbol{x}} .
\end{aligned}
$$

$$
D:(-1,1)
$$

奇函数，

在 $(-1,1)$ 内单调增加．

## 函数的分类：数的分类： $\frac{\text { 函 }}{\text { 数 }}\left\{\begin{array}{l}\text { 基本初等函数 }\left\{\begin{array}{l}\text { 幂函数 } \\ \text { 指数函数 } \\ \text { 对数函数 } \\ \text { 三角函数 } \\ \text { 反三角函数 } \\ \text { 㶗 } \\ \text { 简单函数— } \text { 基本初等函数的有限次 } \\ \text { 四则运算式 } \\ \text { 合函数— 基本初等函数或简单函 } \\ \text { 数的有限次复合运算式 } \\ \text { 非初等函数（一些分段函数，有无穷多项等函数）}\end{array}\right.\end{array}\right.$.

（A）

## 函数及其性质——举例

例1．求下列函数的定义域
（1） $\boldsymbol{f}(\boldsymbol{x})=\frac{1}{\boldsymbol{\operatorname { l n }}(3-\boldsymbol{x})}+\sqrt{49-\boldsymbol{x}^{\mathbf{2}}}$
（2）设 $0<u<1$ 时函数 $f(u)$ 有定义，求 $f(\sin 2 x)$ 的定义域
（3）$f(x)= \begin{cases}\frac{1}{x+1}, & x<0 \\ x, & 0<x<1 \\ 2, & 1 \leq x \leq 2\end{cases}$
例2．设 $f\left(x^{2}+1\right)=x^{4}+5 x^{2}+3$ ，求 $f\left(x^{2}-1\right)$ ．
例3．设 $f\left(\sin \frac{x}{2}\right)=1+\cos x$ ，求 $f\left(\cos \frac{x}{2}\right)$ ．

例4．设 $a f(x)+b f\left(\frac{1}{x}\right)=\frac{c}{x}$ ，其中 $a, b, c$ 为常数，且 $a \neq b$ ，
求证：$f(x)=-f(-x)$ ．
例5．设 $f(x), \varphi(x)$ 及 $\psi(x)$ 为 $(-\infty,+\infty)$ 上的单增函数，
且 $\varphi(x) \leq f(x) \leq \psi(x)$ ，求证：$\varphi[\varphi(x)] \leq f[f(x)] \leq \psi[\psi(x)]$ ．
例6．已知 $y=\left\{\begin{array}{ll}x^{2}, & -1 \leq x<0 \\ \ln x, & 0<x \leq 1 \\ 2 e^{x-1}, & 1<x \leq 2\end{array}\right.$ ，求反函数．
例7．设 $f(x)= \begin{cases}1, & |x|<1 \\ 0, & |x|=1 \\ -1, & |x|>1\end{cases}$，$g(x)=e^{x}$，求 $f[g(x)], g[f(x)]$．

例1．求下列函数的定义域
（1）$f(x)=\frac{1}{\ln (3-x)}+\sqrt{49-x^{2}}$
解：由 $\left\{\begin{array}{l}49-x^{2} \geq 0 \\ 3-x>0 \\ 3-x \neq 1\end{array}\right.$ 得 $\left\{\begin{array}{l}-7 \leq x \leq 7 \\ x<3 \\ x \neq 2\end{array}\right.$
故所求定义域为 $[-7,2) \cup(2,3)$
（2）设 $0<u<1$ 时函数 $f(u)$ 有定义，求 $f(\sin 2 x)$ 的定义域
解：依题意要求 ： $0<\sin 2 x<1$ ，
由此可求得 $x$ 的取值范围，即为定义域．
（3）$f(x)= \begin{cases}\frac{1}{x+1}, & x<0 \\ x, & 0<x<1 \\ 2, & 1 \leq x \leq 2\end{cases}$
解：易知该函数的定义域为：$(-\infty,-1) \cup(-1,0) \cup(0,2]$
例2．设 $f\left(x^{2}+1\right)=x^{4}+5 x^{2}+3$ ，求 $f\left(x^{2}-1\right)$ ．
解：$\because f\left(x^{2}+1\right)=x^{4}+2 x^{2}+1+3 x^{2}+3-1$

$$
=\left(x^{2}+1\right)^{2}+3\left(x^{2}+1\right)-1
$$

$$
\therefore f(x)=x^{2}+3 x-1
$$

故 $f\left(x^{2}-1\right)=\left(x^{2}-1\right)^{2}+3\left(x^{2}-1\right)-1=x^{4}+x^{2}-3$ ．

例3．设 $f\left(\sin \frac{x}{2}\right)=1+\cos x$ ，求 $f\left(\cos \frac{x}{2}\right)$ ．
解：因为

$$
f\left(\sin \frac{x}{2}\right)=1+\cos x=2 \cos ^{2} \frac{x}{2}=2\left(1-\sin ^{2} \frac{x}{2}\right)
$$

所以

$$
f\left(\cos \frac{x}{2}\right)=2\left(1-\cos ^{2} \frac{x}{2}\right)=2 \sin ^{2} \frac{x}{2}=1-\cos x .
$$

例4．设 $a f(x)+b f\left(\frac{1}{x}\right)=\frac{c}{x}$ ，其中 $a, b, c$ 为常数，且 $|a| \neq b$ ，求证：$f(x)=-f(-x)$ ．
证明：以 $\frac{1}{x}$ 代入已知表达式得 $a f\left(\frac{1}{x}\right)+b f(x)=c x$两式联立可求得，

$$
\begin{gathered}
f(x)=\frac{c}{a^{2}-b^{2}}\left(\frac{a}{x}-b x\right) \\
\text { 而 } f(-x)=\frac{c}{a^{2}-b^{2}}\left(-\frac{a}{x}+b x\right)=-\frac{c}{a^{2}-b^{2}}\left(\frac{a}{x}-b x\right)=-f(x) \\
\therefore f(x)=-f(-x)
\end{gathered}
$$

例5．设 $f(x), \varphi(x)$ 及 $\psi(x)$ 为 $(-\infty,+\infty)$ 上的单增函数，且 $\varphi(x) \leq f(x) \leq \psi(x)$ ，求证：$\varphi[\varphi(x)] \leq f[f(x)] \leq \psi[\psi(x)]$ ．

证明：$\forall x_{0}$ ，有 $\varphi\left(x_{0}\right) \leq f\left(x_{0}\right) \leq \psi\left(x_{0}\right)$ ，
由单调性及已知不等式有，

$$
\begin{array}{r}
\varphi\left[\varphi\left(x_{0}\right)\right] \leq \varphi\left[f\left(x_{0}\right)\right] \leq f\left[f\left(x_{0}\right)\right] \\
f\left[f\left(x_{0}\right)\right] \leq f\left[\psi\left(x_{0}\right)\right] \leq \psi\left[\psi\left(x_{0}\right)\right] \\
\therefore \varphi\left[\varphi\left(x_{0}\right)\right] \leq f\left[f\left(x_{0}\right)\right] \leq \psi\left[\psi\left(x_{0}\right)\right]
\end{array}
$$

由 $x_{0}$ 的任意性可知结论成立。

例6．已知 $y=\left\{\begin{array}{ll}x^{2}, & -1 \leq x<0 \\ \ln x, & 0<x \leq 1 \\ 2 e^{x-1}, & 1<x \leq 2\end{array}\right.$ ，求反函数．
解：由 $y=x^{2}$ 得 $y \in(0,1]$ ，
由于 $-1 \leq \boldsymbol{x}<\mathbf{0}, \quad \therefore \boldsymbol{x}=-\sqrt{\boldsymbol{y}}$ ，写成 $\boldsymbol{y}=-\sqrt{\boldsymbol{x}}, \boldsymbol{x} \in(\mathbf{0 , 1}]$
由 $y=\ln x$ 得 $y \in(-\infty, 0]$ ，且 $x=e^{y}$ ，写成 $y=e^{x}, x \in(-\infty, 0]$
再由 $y=2 e^{x-1}$ 得 $x=1+\ln \frac{y}{2}$ ，写成 $y=1+\ln \frac{x}{2}, x \in(2,2 e]$
故所求反函数为 $y= \begin{cases}e^{x}, & -\infty<x \leq 0 \\ -\sqrt{x}, & 0<x \leq 1 \\ 1+\ln \frac{x}{2}, & 2<x \leq 2 e\end{cases}$

例7．设 $f(x)=\left\{\begin{array}{ll}1, & |x|<1 \\ 0, & |x|=1 \\ -1, & |x|>1\end{array}\right.$，$g(x)=e^{x}$，求 $f[g(x)], g[f(x)]$．

$$
\begin{aligned}
& f[g(x)]=\left\{\begin{array}{ll}
1, & |g(x)|<1 \\
0, & |g(x)|=1 \\
-1, & |g(x)|>1
\end{array}=\left\{\begin{array}{ll}
1, & \left|e^{x}\right|<1 \\
0, & \left|e^{x}\right|=1 \\
-1, & \left|e^{x}\right|>1
\end{array}= \begin{cases}1, & x<0 \\
0, & x=0 \\
-1, & x>0\end{cases} \right.\right. \\
& g[f(x)]=e^{f(x)}=\left\{\begin{array}{ll}
e^{1}, & |x|<1 \\
e^{0}, & |x|=1 \\
e^{-1}, & |x|>1
\end{array}= \begin{cases}e, & |x|<1 \\
1, & |x|=1 \\
e^{-1}, & |x|>1\end{cases} \right.
\end{aligned}
$$

## 思考题1

证明定义在对称区间（ $-l, l$ ）上的任意函数可以表示为一个奇函数和一个偶函数的和。
解：设 $f(x)$ 定义在对称区间（－l，l）
令 $g(x)=\frac{f(x)+f(-x)}{2}, h(x)=\frac{f(x)-f(-x)}{2}$
可知 $g(-x)=\frac{f(-x)+f(x)}{2}=g(x)$ ，即 $g(x)$ 是偶函数
$h(-x)=\frac{f(-x)-f(x)}{2}=-h(x)$ ，即 $h(x)$ 是奇函数

故 $f(x)=g(x)+h(x)$ ，命题得证。

## 练习

三、以下函数中哪些是初等函数，说明理由：
1．$y=|x|$
2．$y=x^{x}(x>0)$
3．$y=\left\{\begin{array}{cc}-\sin x & 0 \leq x \leq \pi \\ \sin x & \pi<x \leq 2 \pi\end{array}\right.$
4．$y= \begin{cases}1 & x \text { 为有理数 } \\ 0 & x \text { 为无理数 }\end{cases}$

解：（1）是初等函数，因为 $y=|x|=\sqrt{x^{2}}$ ，是复合函数，且能用一个解析式表示；
（2）是初等函数，因为幂指函数 $y=x^{x}=e^{x \ln x}$ 是复合函数。
（3）是初等函数，因为 $y=\left\{\begin{array}{cc}-\sin x & 0 \leq x \leq \pi \\ \sin x & \pi<x \leq 2 \pi\end{array}=-\sqrt{\sin ^{2} x}, x \in[0,2 \pi]\right.$ 是复合函数，

## 且能用一个解析式表示。

（4）不是初等函数，因为 $y=\left\{\begin{array}{ll}1 & x \text { 为有理数 } \\ 0 & x \text { 为无理数 }\end{array}\right.$ 是分段函数，但不能用一个解析式表示。

四、举出一个定义域不包含区间的初等函数．

分析：注意定义域与定义区间的区别。

解：$y=\sqrt{\sin ^{2} x-1}$ ，定义域：$\left\{n \pi+\frac{\pi}{2}\right\}, n=0, \pm 1, \pm 2, \cdots+$
