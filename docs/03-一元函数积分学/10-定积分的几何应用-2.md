
## 第3章 一元函数积分学

## 3.3 定积分的应用

## 一、立体体积

## 平行截面面积为已知的立体的体积

如果一个立体，我们知道该立体上垂直于一定轴的各个截面面积，那么，这个立体的体积也可用定积分来计算。
$\boldsymbol{A}(\boldsymbol{x})$ 表示过点 $\boldsymbol{x}$ 且垂直于 $\boldsymbol{x}$ 轴

的截面面积，$A(x)$ 为 $x$ 的已知连续函数
积分变量 $x \in[a, b], \quad d V=A(x) d x$ ，
立体体积 $V=\int_{a}^{b} A(x) d x$ ．

注意：

若立体垂直于 $y$ 轴的截面面积为 $B(y)$ ，则

$$
V=\int_{c}^{d} B(y) d y .
$$

## 计算立体体积习例

例1 一平面经过半径为 $\boldsymbol{R}$ 的圆柱体的底圆中心，并与底面交成角 $\alpha$ ，计算这平面截圆柱体所得立体的体积。

例2 求以半径为 $\boldsymbol{R}$ 的圆为底、平行且等于底圆半径的线段为顶、高为 $\boldsymbol{h}$ 的正劈锥体的体积。

例1 一平面经过半径为 $\boldsymbol{R}$ 的圆柱体的底圆中心，并与底面交成角 $\alpha$ ，计算这平面截圆柱体所得立体的体积．

解 取坐标系如图
底圆方程为 $\boldsymbol{x}^{\mathbf{2}}+\boldsymbol{y}^{\mathbf{2}}=\boldsymbol{R}^{\mathbf{2}}$
垂直于 $\boldsymbol{x}$ 轴的截面为直角三角形积分变量 $x \in[-R, R]$ ，

截面面积 $A(x)=\frac{1}{2}\left(R^{2}-x^{2}\right) \tan \alpha$ ，
立体体积 $V=\frac{1}{2} \int_{-R}^{R}\left(R^{2}-x^{2}\right) \tan \alpha d x=\frac{2}{3} R^{3} \tan \alpha$ ．
（20）（10）（№）（－）

例2 求以半径为 $\boldsymbol{R}$ 的圆为底、平行且等于底圆半径的线段为顶、高为 $h$ 的正劈锥体的体积．

解 取坐标系如图
底圆方程为 $x^{2}+y^{2}=R^{2}$ ，

垂直于 $x$ 轴的截面为等腰三角形
积分变量 $x \in[-R, R]$ ，
截面面积 $A(x)=h \cdot y=h \sqrt{R^{2}-x^{2}}$
立体体积 $V=h \int_{-R}^{R} \sqrt{R^{2}-x^{2}} d x=\frac{1}{2} \pi R^{2} h$ ．

## 二、平面曲线弧长

设 $\boldsymbol{A} 、 \boldsymbol{B}$ 是曲线弧上的两个端点，在弧上插入分点

$$
\begin{aligned}
& A=M_{0}, M_{1}, \cdots M_{i}, \\
& \cdots, M_{n-1}, M_{n}=B
\end{aligned}
$$

并依次连接相邻分点得一内接折线，当分点的数目无限增加且每个小弧段都缩向一点时，此折线的长 $\sum_{i=1}^{n}\left|\boldsymbol{M}_{i-1} \boldsymbol{M}_{i}\right|$ 的极限存在，则称此极限为曲线弧 $\boldsymbol{A B}$ 的弧长．

1．直角坐标情形
设曲线弧为 $y=f(x)$
$(a \leq x \leq b)$ ，其中 $f(x)$
在 $[\boldsymbol{a}, \boldsymbol{b}]$ 上有一阶连续导数
取积分变量为 $x$ ，在 $[a, b]$

上任取小区间 $[x, x+d x]$ ，
以对应小切线段的长代替小弧段的长
小切线段的长 $\sqrt{(d x)^{2}+(d y)^{2}}=\sqrt{1+y^{\prime 2}} d x$
弧长元素 $d s=\sqrt{1+y^{\prime 2}} d x$ 弧长 $s=\int_{a}^{b} \sqrt{1+y^{\prime 2}} d x$ 。
（3）（14）（№）（3）

## 2.参数方程情形

曲线弧为 $\left\{\begin{array}{l}x=\varphi(t) \\ y=\psi(t)\end{array}, \quad(\alpha \leq t \leq \beta)\right.$
其中 $\varphi(t), \psi(t)$ 在 $[\alpha, \beta]$ 上具有连续导数．

$$
\begin{aligned}
d s & =\sqrt{(d x)^{2}+(d y)^{2}}=\sqrt{\left[\varphi^{\prime 2}(t)+\psi^{\prime 2}(t)\right](d t)^{2}} \\
& =\sqrt{\varphi^{\prime 2}(t)+\psi^{\prime 2}(t)} d t
\end{aligned}
$$

弧长 $s=\int_{\alpha}^{\beta} \sqrt{\varphi^{\prime 2}(t)+\psi^{\prime 2}(t)} d t$ 。

3．极坐标情形
曲线弧为 $r=r(\theta) \quad(\alpha \leq \theta \leq \beta)$

其中 $\varphi(\theta)$ 在 $[\alpha, \beta]$ 上具有连续导数．
$\because\left\{\begin{array}{l}x=r(\theta) \cos \theta \\ y=r(\theta) \sin \theta\end{array} \quad(\alpha \leq \theta \leq \beta)\right.$
$\therefore d s=\sqrt{(d x)^{2}+(d y)^{2}}=\sqrt{r^{2}(\theta)+r^{\prime 2}(\theta)} d \theta$ ，
弧长 $s=\int_{\alpha}^{\beta} \sqrt{r^{2}(\theta)+r^{\prime 2}(\theta)} d \theta$ 。

## 计算曲线弧长习例

例3 计算曲线 $y=\frac{2}{3} x^{\frac{3}{2}}$ 上相应于 $x$ 从 $a$ 到 $b$ 的一段弧的长度．
例4 计算曲线 $y=\int_{0}^{\frac{x}{n}} n \sqrt{\sin \theta} d \theta$ 的弧长 $(0 \leq x \leq n \pi)$ ．
例5 求星形线 $x^{\frac{2}{3}}+y^{\frac{2}{3}}=a^{\frac{2}{3}}(a>0)$ 的全长．
例6 求极坐标系下曲线 $r=a\left(\sin \frac{\theta}{3}\right)^{3}$ 的长．

$$
(a>0)(0 \leq \theta \leq 3 \pi)
$$

例7 求阿基米德螺线 $\boldsymbol{r}=\boldsymbol{a} \theta(\boldsymbol{a}>\mathbf{0})$ 上相应于 $\theta$ 从 0 到 $2 \pi$ 的弧长．

例3 计算曲线 $\boldsymbol{y}=\frac{\mathbf{2}}{\mathbf{3}} \boldsymbol{x}^{\frac{\mathbf{3}}{\mathbf{2}}}$ 上相应于 $\boldsymbol{x}$ 从 $\boldsymbol{a}$ 到 $\boldsymbol{b}$ 的一段弧的长度．
解 $\because y^{\prime}=x^{\frac{1}{2}}$ ，

$$
\therefore d s=\sqrt{1+\left(x^{\frac{1}{2}}\right)^{2}} d x=\sqrt{1+x} d x
$$

所求弧长为

$$
s=\int_{a}^{b} \sqrt{1+x} d x=\frac{2}{3}\left[(1+b)^{\frac{3}{2}}-(1+a)^{\frac{3}{2}}\right]
$$

2．

例4 计算曲线 $y=\int_{0}^{\frac{x}{n}} n \sqrt{\sin \theta} d \theta$ 的弧长 $(0 \leq x \leq n \pi)$ ．
解 $y^{\prime}=n \sqrt{\sin \frac{x}{n}} \cdot \frac{1}{n}=\sqrt{\sin \frac{x}{n}}$ ，

$$
\begin{aligned}
s= & \int_{a}^{b} \sqrt{1+y^{\prime 2}} d x=\int_{0}^{n \pi} \sqrt{1+\sin \frac{x}{n}} d x \\
& \xlongequal{x=n t} \int_{0}^{\pi} \sqrt{1+\sin t} \cdot n d t \\
= & n \int_{0}^{\pi} \sqrt{\left(\sin \frac{t}{2}\right)^{2}+\left(\cos \frac{t}{2}\right)^{2}+2 \sin \frac{t}{2} \cos \frac{t}{2}} d t \\
= & n \int_{0}^{\pi}\left(\sin \frac{t}{2}+\cos \frac{t}{2}\right) d t=4 n
\end{aligned}
$$

例5 求星形线 $x^{3}+y^{3}=a^{3}(a>0)$ 的全长．
解 星形线的参数方程为 $\left\{\begin{array}{l}x=a \cos ^{3} t \\ y=a \sin ^{3} t\end{array}(0 \leq t \leq 2 \pi)\right.$
根据对称性 $s=4 s_{1}$ 第一象限部分的弧长

$$
\begin{aligned}
& =4 \int_{0}^{\frac{\pi}{2}} \sqrt{\left(x^{\prime}\right)^{2}+\left(y^{\prime}\right)^{2}} d t \\
& =4 \int_{0}^{\frac{\pi}{2}} 3 a \sin t \cos t d t=6 a
\end{aligned}
$$

例6 求极坐标系下曲线 $r=a\left(\sin \frac{\theta}{3}\right)^{3}$ 的长．

$$
(a>0)(0 \leq \theta \leq 3 \pi)
$$

解

$$
\begin{aligned}
\because r^{\prime} & =3 a\left(\sin \frac{\theta}{3}\right)^{2} \cdot \cos \frac{\theta}{3} \cdot \frac{1}{3}=a\left(\sin \frac{\theta}{3}\right)^{2} \cdot \cos \frac{\theta}{3} \\
\therefore s & =\int_{\alpha}^{\beta} \sqrt{r^{2}(\theta)+r^{\prime 2}(\theta)} d \theta \\
& =\int_{0}^{3 \pi} \sqrt{a^{2}\left(\sin \frac{\theta}{3}\right)^{6}+a^{2}\left(\sin \frac{\theta}{3}\right)^{4}\left(\cos \frac{\theta}{3}\right)^{2}} d \theta \\
& =a \int_{0}^{3 \pi}\left(\sin \frac{\theta}{3}\right)^{2} d \theta=\frac{3}{2} \pi a .
\end{aligned}
$$

例7 求阿基米德螺线 $\boldsymbol{r}=\boldsymbol{a} \boldsymbol{\theta} \quad(\boldsymbol{a}>\mathbf{0})$ 上相应于 $\boldsymbol{\theta}$ 从 $\mathbf{0}$到 $2 \pi$ 的弧长。

解 $\because r^{\prime}=a$ ，

$$
\begin{aligned}
\therefore s & =\int_{\alpha}^{\beta} \sqrt{r^{2}(\theta)+r^{\prime 2}(\theta)} d \theta \\
& =\int_{0}^{2 \pi} \sqrt{a^{2} \theta^{2}+a^{2}} d \theta=a \int_{0}^{2 \pi} \sqrt{\theta^{2}+1} d \theta \\
& =\frac{a}{2}\left[2 \pi \sqrt{1+4 \pi^{2}}+\ln \left(2 \pi+\sqrt{1+4 \pi^{2}}\right)\right]
\end{aligned}
$$

## 三、变力沿直线所作的功

## 变力作功包括有：电场力作功、气体压力作功、克服阻力作功、万有引力作功、弹力作功等。

由物理学知道，如果物体在作直线运动的过程中有一个不变的力 $\boldsymbol{F}$ 作用在这物体上，且这力的方向与物体的运动方向一致，那么，在物体移动了距离 $s$ 时，力 $F$ 对物体所作的功为 $W=F \cdot s$ 。

如果物体在运动的过程中所受的力是变化的，就不能直接使用此公式，而采用＂微元法＂思想。

[^0]设变力 $f(x)$ ：其方向沿 $x$ 轴正向，大小随 $x$ 值的变化而变化。
变力 $f(x)$ 推动物体，从点 $x=a$ 处沿 $x$ 轴正向运动到点 $x=b$ 处 $(a<b)$ 所作的功为：$\forall x \in(a, b], \Delta x>0$ ．

当 $\Delta x$ 很小时，可视物体在区间 $[x, x+\Delta x]$ 上，以变力在点 $x$ 处的值 $f(x)$ 按常力 作功，其值为

$$
\Delta W=f(x) \Delta x .
$$

于是，变力沿直线作功问题的微分元素为： $\mathrm{d} W=f(x) \mathrm{d} x$ 。

由于功对区间具有可加性，故变力 $f(x)$ 沿直线移动物体所做的功为：

积分区间：$x \in[a, b]$ ．
微分元素： $\mathrm{d} W=f(x) \mathrm{d} x$.

变力作功的几何表示

功的计算：$W=\int_{a}^{b} \mathrm{~d} W=\int_{a}^{b} f(x) \mathrm{d} x$ 。

解题思路：
（1）适当选取坐标系及积分变量；
（2）写出功元素 $\mathrm{d} w$ 的表达式；
（以不变代变，其中用了公式 $w=F \cdot s$ ）
（3）列出定积分并求值即得 $w$ ．

## 变力做功习例

例 8．把一个带 $+\boldsymbol{q}$ 电量的点电荷放在 $\boldsymbol{r}$ 轴上坐标原点处，它产生一个电场．这个电场对周围的电荷有作用力。由物理学知道，如果一个单位正电荷放在这个电场中距离原点为 $\boldsymbol{r}$ 的地方，那么电场对它的作用力的大小为 $\boldsymbol{F}=\boldsymbol{k} \frac{\boldsymbol{q}}{\boldsymbol{r}^{2}}$（ $\boldsymbol{k}$ 是常数），当这个单位正电荷在电场中从 $r=a$ 处沿 $r$ 轴移动到 $r=b$ 处时，计算电场力 $\boldsymbol{F}$ 对它所作的功。

例 9．一圆柱形蓄水池高为 $\mathbf{5}$ 米，底半径为 $\mathbf{3}$ 米，池内盛满了水。问要把池内的水全部吸出，需作多少功？
（28）（10）（№）（8）

例10（1）半球形贮水池，贮满水，从池中把水抽出，问作多少功？
（2）若半球形贮水池平底在下，问作多少功？
例11 半径为 $R$ ，比重为 $\sigma$（大于1）的球沉入深为 $H(>2 R)$ 的水池底，现将其从水中取出，问需作多少功？

例12 用铁锤把钉子钉入木板，设木板对铁钉的阻力与铁钉进入木板的深度成正比，铁锤在第一次锤击时将铁钉击入1厘米，若每次锤击所作的功相等，问第 $n$ 次锤击时又将铁钉击入多少？

例 8 把一个带 $+\boldsymbol{q}$ 电量的点电荷放在 $\boldsymbol{r}$ 轴上坐标原点处，它产生一个电场．这个电场对周围的电荷有作用力．由物理学知道，如果一个单位正电荷放在这个电场中距离原点为 $r$ 的地方，那么电场对它的作用力的大小为 $\boldsymbol{F}=\boldsymbol{k} \frac{\boldsymbol{q}}{\boldsymbol{r}^{2}}$（ $\boldsymbol{k}$ 是常数），当这个单位正电荷在电场中从 $\boldsymbol{r}=\boldsymbol{a}$ 处沿 $\boldsymbol{r}$ 轴移动到 $\boldsymbol{r}=\boldsymbol{b}$ 处时，计算电场力 $\boldsymbol{F}$ 对它所作的功．
解
取 $r$ 为积分变量，$r \in[a, b]$ ，

取任一小区间 $[\boldsymbol{r}, \boldsymbol{r}+\boldsymbol{d} \boldsymbol{r}]$ ，功元素 $d w=\frac{k q}{r^{2}} d r$ ，
所求功为 $w=\int_{a}^{b} \frac{k q}{r^{2}} d r=k q\left[-\frac{1}{r}\right]_{a}^{b}=k q\left(\frac{1}{a}-\frac{1}{b}\right)$ ．

例9 一圆柱形蓄水池高为 5 米，底半径为 3 米，池内盛满了水。问要把池内的水全部吸出，需作多少功？

解 建立坐标系如图

取 $x$ 为积分变量， $x \in[0,5]$

取任一小区间 $[x, x+d x]$ ，

这一薄层水的重力为

$$
9.8 \pi \cdot 3^{2} d x
$$

功元素为 $d w=88.2 \pi \cdot x \cdot d x$ ，

$$
\begin{aligned}
\therefore w & =\int_{0}^{5} 88.2 \pi \cdot x \cdot d x \\
& =88.2 \pi\left[\frac{x^{2}}{2}\right]_{0}^{5} \approx 3462 \text { (千焦). }
\end{aligned}
$$

例10（1）半球形贮水池，贮满水，从池中把水抽出，问作多少功？
（2）若半球形贮水池平底在下，问作多少功？
解（1）如图所示建立坐标系，则边界曲线方程为 $x^{2}+y^{2}=R^{2}$选 $x$ 为积分变量，$x \in[0, R] \forall[x, x+d x] \subset[0, R]$,

这一薄层水的重力为 $\boldsymbol{g} \boldsymbol{\pi} \boldsymbol{y}^{\mathbf{2}} \boldsymbol{d} \boldsymbol{x} \cdot \mathbf{1}=g \pi\left(R^{2}-x^{2}\right) d x$

$$
\begin{aligned}
& \text { 故 } d w=g \pi x\left(R^{2}-x^{2}\right) d x \\
& \therefore \boldsymbol{w}=\int_{0}^{\boldsymbol{R}} \boldsymbol{g} \boldsymbol{\pi} \boldsymbol{x}\left(\boldsymbol{R}^{\mathbf{2}}-\boldsymbol{x}^{\mathbf{2}}\right) \boldsymbol{d} \boldsymbol{x} .
\end{aligned}
$$

（2）如图所示建立坐标系，
则边界曲线方程为 $x^{2}+y^{2}=R^{2}$选 $y$ 为积分变量，$y \in[0, R]$

$$
\forall[y, y+d y] \subset[0, R],
$$

这一薄层水的重力为 $g \pi x^{2} d y \cdot 1=g \pi\left(R^{2}-y^{2}\right) d y$

$$
\begin{aligned}
& \text { 故 } d w=g \pi(R-y)\left(R^{2}-y^{2}\right) d y \\
& \therefore \boldsymbol{w}=\int_{0}^{\boldsymbol{R}} \boldsymbol{g} \boldsymbol{\pi}(\boldsymbol{R}-\boldsymbol{y})\left(\boldsymbol{R}^{\mathbf{2}}-\boldsymbol{y}^{\mathbf{2}}\right) \boldsymbol{d y} .
\end{aligned}
$$

注意：也可建立另一如图所示的坐标系，则边界曲线方程为

$$
(x-R)^{2}+y^{2}=R^{2}
$$

选 $x$ 为积分变量，$x \in[0, R]$

$$
\forall[x, x+d x] \subset[0, R],
$$

这一薄层水的重力为 $g \pi y^{2} d x \cdot 1=\boldsymbol{g} \boldsymbol{\pi}\left[\boldsymbol{R}^{\mathbf{2}}-(\boldsymbol{x}-\boldsymbol{R})^{\mathbf{2}}\right] \boldsymbol{d} \boldsymbol{x}$故 $d w=g \pi x\left[R^{2}-(x-R)^{2}\right] d x$

$$
\therefore w=\int_{0}^{R} g \pi x\left(2 R x-x^{2}\right) d x .
$$

例11 半径为 $R$ ，比重为 $\sigma$（大于1）的球沉入深为 $H(>2 R)$ 的水池底，现将其从水中取出，问需作多少功？

解 如图所示建立坐标系，则边界曲线方程为

$$
(x-R)^{2}+y^{2}=R^{2}
$$

选 $x$ 为积分变量，$x \in[0,2 R] \forall[x, x+d x] \subset[0,2 R]$,将其取出水面总行程为 $\boldsymbol{H}$ ，

在水中行程为 $\boldsymbol{H}-\mathbf{2} \boldsymbol{R}+\boldsymbol{x}$ ，在水外行程为 $H-(H-2 R+x)^{2}$ 面

在水中作功的力为 $g \pi \sigma y^{2} d x-g \pi y^{2} d x=g \pi(\sigma-1) y^{2} d x$在水外作功的力为 $g \pi \sigma y^{2} d x$此时功元素为两部分之和（水中与水外）

$$
\begin{aligned}
& \therefore d w= g \pi(\sigma-1) y^{2}(H-2 R+x) d x \\
&+g \pi \sigma y^{2}(2 R-x) d x \\
&= g\left[\pi H(\sigma-1) y^{2}+\pi y^{2}(2 R-x)\right] d x \\
&=\left.g\left\{\left[2 \pi(\sigma-1) H R+4 \pi R^{2}\right] x-[\pi(\sigma-1) H+4 \pi R] x^{2}+\pi x^{3}\right)\right\} d x \\
& \therefore w=\int_{0}^{2 R} d w=\frac{4}{3} g \pi R^{3}[(\sigma-1) H+R] .
\end{aligned}
$$

例12 用铁锤把钉子钉入木板，设木板对铁钉的阻力与铁钉进入木板的深度成正比，铁锤在第一次锤击时将铁钉击入 1 厘米，若每次锤击所作的功相等，问第 $n$ 次锤击时又将铁钉击入多少？

解
设木板对铁钉的阻力为 $f(x)=k x$ ， $d w=f(x) d x=k x d x$,
第一次锤击时所作的功为 $w_{1}=\int_{0}^{1} k x d x=\frac{\boldsymbol{k}}{\mathbf{2}}$ ，设 $\boldsymbol{n}$ 次击入的总深度为 $\boldsymbol{n}$ 厘米
$\boldsymbol{n}$ 次锤击所作的总功为 $w_{h}=\int_{0}^{h} f(x) d x$ ．

$$
w_{h}=\int_{0}^{h} k x d x=\frac{k h^{2}}{2}
$$

依题意知，每次锤击所作的功相等。

$$
w_{h}=n w_{1} \Rightarrow \frac{k h^{2}}{2}=n \cdot \frac{k}{2},
$$

$\boldsymbol{n}$ 次击入的总深度为 $\boldsymbol{n}=\sqrt{\boldsymbol{n}}$ ，

第 $\boldsymbol{n}$ 次击入的深度为 $\sqrt{\boldsymbol{n}}-\sqrt{\boldsymbol{n}-1}$ ．

## 四、液体压力

由物理学知道，在水深为 $\boldsymbol{h}$ 处的压强为 $\boldsymbol{p}=\gamma \boldsymbol{h}$ ，这里 $\gamma$ 是水的比重。如果有一面积为 $\boldsymbol{A}$ 的平板水平地放置在水深为 $\boldsymbol{h}$ 处，那么，平板一侧所受的水压力为 $\boldsymbol{P}=\boldsymbol{p} \cdot \boldsymbol{A}$ 。

如果平板垂直放置在水中，由于水深不同的点处压强 $\boldsymbol{p}$ 不相等，平板一侧所受的水压力就不能直接使用此公式，而采用＂微元法＂思想。

[^1]解题思路：
（1）适当选取坐标系及积分变量；
（2）写出液体压力元素 $\mathbf{d} \boldsymbol{F}$ 的表达式；
（以不变代变，其中用了公式 $F=p \cdot A, p=\gamma h$ ）
（3）列出定积分并求值即得 $F$ ．

例 13 一个横放着的圆柱形水桶，桶内盛有半桶水，设桶的底半径为 $\boldsymbol{R}$ ，水的比重为 $\gamma$ ，计算桶的一端面上所受的压力。

例 14 将直角边各为 $a$ 及 $2 a$ 的直角三角形薄板垂直地浸人水中，斜边朝下，直角边的长边与水面平行，且该边到水面的距离恰等于该边的边长，求薄板所受的侧压力。

例 13 一个横放着的圆柱形水桶，桶内盛有半桶水，设桶的底半径为 $\boldsymbol{R}$ ，水的比重为 $\gamma$ ，计算桶的一端面上所受的压力。

解 在端面建立坐标系如图

取 $x$ 为积分变量，$x \in[0, R]$
取任一小区间 $[x, x+d x]$
小矩形片上各处的压强近似相等

$$
p=\gamma x,
$$

小矩形片的面积为 $2 \sqrt{R^{2}-x^{2}} d x$ ．

$$
F=p \cdot A, p=\gamma h
$$

小矩形片的压力元素为 $d F=2 \gamma x \sqrt{R^{2}-x^{2}} d x$端面上所受的压力

$$
\begin{align*}
F & =\int_{0}^{R} 2 \gamma x \sqrt{R^{2}-x^{2}} d x \\
& =-\gamma \int_{0}^{R} \sqrt{R^{2}-x^{2}} d\left(R^{2}-x^{2}\right) \\
& =-\gamma\left[\frac{2}{3}\left(\sqrt{R^{2}-x^{2}}\right)^{3}\right]_{0}^{R}=\frac{2 \gamma}{3} R^{3} \tag{8}
\end{align*}
$$

$$
F=p \cdot A, p=\gamma h
$$

例 14 将直角边各为 $\boldsymbol{a}$ 及 $\mathbf{2 a}$ 的直角三角形薄板垂直地浸人水中，斜边朝下，直角边的长边与水面平行，且该边到水面的距离恰等于该边的边长，求薄板所受的侧压力。
解 建立坐标系如图斜边方程为 $y=2(a-x)$ ，积分变量为 $x \in[0, a]$ ， $\forall[x, x+d x] \subset[0, a]$ ，小面积 $2(a-x) d x$ ， $\therefore d F=(x+2 a) \cdot 2(a-x) \cdot 1 \cdot \gamma d x$
$\therefore F=\int_{0}^{a} 2(x+2 a)(a-x) \gamma d x=\frac{7}{3} \gamma a^{3}$.

## 五、万有引力

由物理学知道，质量分别为 $\boldsymbol{m}_{1}, \boldsymbol{m}_{2}$ 相距为 $r$ 的两个质点间的引力的大小为 $F=k \frac{m_{1} m_{2}}{r^{2}}$ ，其中 $k$ 为引力系数，引力的方向沿着两质点的连线方向．

如果要计算一根细棒对一个质点的引力，那么，由于细棒上各点与该质点的距离是变化的，且各点对该质点的引力方向也是变化的，就不能用此公式计算。

解题思路：
（1）适当选取坐标系及积分变量；
（2）写出引力元素 $\mathbf{d} \boldsymbol{F}$ 的表达式；
（以不变代变，其中用了公式 $F=k \frac{m_{1} \cdot m_{2}}{r^{2}}$ ）
（3）列出定积分并求值即得 $F$ ．

## 万有引力习例

例 15 有一长度为 $l$ 、线密度为 $\rho$ 的均匀细棒，在其中垂线上距棒 $\boldsymbol{a}$ 单位处有一质量为 $\boldsymbol{m}$ 的质点 $\boldsymbol{M}$ ，计算该棒对质点 $\boldsymbol{M}$ 的引力。

解 建立坐标系如图
取 $y$ 为积分变量 $y \in\left[-\frac{l}{2}, \frac{l}{2}\right]$ ，
取任一小区间 $[y, y+d y]$
将典型小段近似看成质点

小段与质点的距离为 $r=\sqrt{a^{2}+y^{2}}$ ，引力元素 $\quad d F=k \frac{m \rho d y}{a^{2}+y^{2}}$ ，

又力的方向为 $\vec{s}=\{-a, y\}$ ，

$$
\cos \alpha=\frac{-a}{\sqrt{a^{2}+y^{2}}}, \quad \sin \alpha=\frac{y}{\sqrt{a^{2}+y^{2}}},
$$

水平方向的分力元素

$$
d F_{x}=d F \cdot \cos \alpha=-k \frac{a m \rho d y}{\left(a^{2}+y^{2}+\frac{3}{2}\right)^{2}},
$$

$$
F_{x}=-\int_{-\frac{l}{2}}^{\frac{l}{2}} k \frac{a m \rho d y}{\left(a^{2}+y^{2}\right)^{\frac{3}{2}}}=\frac{-2 k m \rho l}{a\left(4 a^{2}+l^{2}\right)^{\frac{1}{2}}},
$$

铅直方向的分力元素

$$
\begin{align*}
& d F_{y}=d F \cdot \sin \alpha=k \frac{m \rho y d y}{\left(a^{2}+y^{2}\right)^{\frac{3}{2}}} \\
& F_{y}=\int_{-\frac{l}{2}}^{\frac{l}{2}} k \frac{a m y d y}{\left(a^{2}+y^{2}\right)^{\frac{3}{2}}}=0 \\
& \therefore \vec{F}=\left\{\frac{-2 k m \rho l}{a\left(4 a^{2}+l^{2}\right)^{\frac{1}{2}}}, 0\right\} \tag{回}
\end{align*}
$$

## 六、函数的平均值与均方根

1．平均值
实例：用某班所有学生的考试成绩的算术平均值来描述这个班的成绩的概貌。

$$
\bar{y}=\frac{y_{1}+y_{2}+\cdots y_{n}}{n}
$$

⟶算术平均值公式
⟶只适用于有限个数值
问题：求气温在一昼夜间的平均温度．
入手点：连续函数 $f(x)$ 在区间 $[a, b]$ 上的平均值．
讨论思想：分割、求和、取极限．
（1）分割：把区间 $[a, b]$ 分成 $n$ 等分

$$
\begin{aligned}
& a=x_{0}<x_{1}<x_{2}<\cdots<x_{n-1}<x_{n}=b, \\
& \text { 每个小区间的长度 } \Delta x=\frac{b-a}{n} ;
\end{aligned}
$$

（2）求和：设各分点处的函数值为 $y_{0}, y_{1}, y_{2}, \cdots, y_{n}$函数 $f(x)$ 在区间 $[a, b]$ 上的平均值近似为

$$
\frac{y_{0}+y_{1}+y_{2}+\cdots y_{n-1}}{n} ;
$$

（3）取极限：每个小区间的长度趋于零．

函数 $f(x)$ 在区间 $[a, b]$ 上的平均值为

$$
\begin{aligned}
\bar{y} & =\lim _{n \rightarrow \infty} \frac{y_{0}+y_{1}+y_{2}+\cdots y_{n-1}}{n} \\
& =\lim _{n \rightarrow \infty} \frac{y_{0}+y_{1}+y_{2}+\cdots y_{n-1}}{b-a} \frac{b-a}{n}=\Delta x \\
& =\frac{1}{b-a} \lim _{\Delta x \rightarrow 0} \sum_{i=1}^{n} y_{i-1} \Delta x=\frac{1}{b-a} \lim _{\Delta x \rightarrow 0} \sum_{i=1}^{n} f\left(x_{i-1}\right) \Delta x,
\end{aligned}
$$

$\bar{y}=\frac{1}{b-a} \sqrt{\int_{a}^{b} f(x) d x} \longrightarrow$ 几何平均值公式区间长度 $\quad \longrightarrow=(b-a) \bar{y}=(b-a) f(\xi)$

## 函数平均值习例

例16 计算纯电阻电路中正弦交流电 $i=I_{m} \sin \omega t$ 在一个周期上的功率的平均值（简称平均功率）。

解 设电阻为 $R$ ，
则电路中的电压为 $u=i R=I_{m} R \sin \omega t$ ，
功率 $p=u i=I_{m}^{2} R \sin ^{2} \omega t$,
一个周期区间 $\left[0, \frac{2 \pi}{\omega}\right]$ ，
平均功率 $\bar{p}=\frac{1}{\frac{2 \pi}{\omega}} \int_{0}^{\frac{2 \pi}{\omega}} I_{m}^{2} R \sin ^{2} \omega t d t$

$$
\begin{aligned}
\bar{p} & =\frac{1}{\frac{2 \pi}{\omega}} \int_{0}^{\frac{2 \pi}{\omega}} I_{m}^{2} R \sin ^{2} \omega t d t=\frac{I_{m}^{2} R}{2 \pi} \int_{0}^{\frac{2 \pi}{\omega}} \sin ^{2} \omega t d(\omega t) \\
& =\frac{I_{m}^{2} R}{4 \pi} \int_{0}^{2 \pi}(1-\cos 2 \omega t) d(\omega t) \\
& =\frac{I_{m}^{2} R}{4 \pi}\left[\omega t-\frac{\sin 2 \omega t}{2}\right]_{0}^{\frac{2 \pi}{\omega}}=\frac{I_{m}^{2} R}{4 \pi} \cdot 2 \pi=\frac{I_{m}^{2} R}{2}=\frac{I_{m} U_{m}}{2} . \\
& \left(U_{m}=I_{m} R\right)
\end{aligned}
$$

结论：纯电阻电路中正弦交流电的平均功率等于电流、电压的峰值的乘积的二分之一。

[^2]
## 2.均方根

通常交流电器上标明的功率就是平均功率．交流电器上标明的电流值都是一种特定的平均值，习惯上称为有效值。

周期性非恒定电流 $i$（如正弦交流电）的有效值规定如下：当 $i(t)$ 在它的一个周期 $T$ 内在负载电阻 $\boldsymbol{R}$ 上消耗的平均功率，等于取固定值 $\boldsymbol{I}$ 的恒定电流在 $\boldsymbol{R}$ 上消耗的功率时，称这个值 $\boldsymbol{I}$ 为 $i(t)$ 的有效值．

有效值计算公式的推导：
固定值为 $\boldsymbol{I}$ 的恒定电流在 $\boldsymbol{R}$ 上消耗的功率为 $\boldsymbol{I}^{2} \boldsymbol{R}$ ，电流 $i(t)$ 在 $R$ 上消耗的功率为 $i^{2}(t) R$ ，它在 $[0, T]$ 上的平均功率为 $\frac{1}{T} \int_{0}^{T} i^{2}(t) R d t$ ，按定义有 $I^{2} R=\frac{1}{T} \int_{0}^{T} i^{2}(t) R d t$,

$$
\begin{equation*}
\therefore I^{2}=\frac{1}{T} \int_{0}^{T} i^{2}(t) d t \quad \text { 即 } I=\sqrt{\frac{1}{T} \int_{0}^{T} i^{2}(t) d t} \text {. } \tag{2}
\end{equation*}
$$

正弦交流电 $i(t)=I_{m} \sin \omega t$ 的有效值为

$$
\begin{aligned}
I & =\sqrt{\frac{1}{\frac{2 \pi}{\omega}} \int_{0}^{\frac{2 \pi}{\omega}} I_{m}^{2} \sin ^{2} \omega t d t}=\sqrt{\frac{I_{m}^{2}}{2 \pi} \int_{0}^{\frac{2 \pi}{\omega}} \sin ^{2} \omega t d(\omega t)} \\
& =\sqrt{\frac{I_{m}^{2}}{4 \pi}\left[\omega t-\frac{\sin 2 \omega t}{2}\right]_{0}^{\frac{2 \pi}{\omega}}}=\frac{I_{m}}{\sqrt{2}} .
\end{aligned}
$$

结论：正弦交流电的有效值等于电流的峰值的 $\frac{1}{\sqrt{2}}$ ．
函数 $f(x)$ 在 $[a, b]$ 上的均方根 $\sqrt{\frac{1}{b-a} \int_{a}^{b} f^{2}(x) d x}$ ．

## 内容小结

1．已知平行截面面积函数的立体体积

$$
V=\int_{a}^{b} A(x) \mathrm{d} x
$$

2．平面曲线的弧长
弧微分： $\mathrm{d} s=\sqrt{(\mathrm{d} x)^{2}+(\mathrm{d} y)^{2}}$

注意：求弧长时积分上下限必须上大下小

曲线方程 $\left\{\begin{array}{l}\text { 直角坐标方程 } \\ \text { 参数方程方程 } \\ \text { 极坐标方程 } \mathrm{d} s=\sqrt{r^{2}(\theta)+r^{\prime 2}(\theta)} \mathrm{d} \theta\end{array}\right.$
3．功、液体压力、万有引力、平均值、均方根

[^0]:    （3）（A）（A）（A）

[^1]:    （3）（A）（A）（A）

[^2]:    （8）（14）（－1）

<!-- 补充内容来自高数上版本 -->
## 第3章 一元函数积分学
## 3.3 定积分的应用
## 一、立体体积
## 平行截面面积为已知的立体的体积
## 计算立体体积习例
## 二、平面曲线弧长
并依次连接相邻分点得一内接折线，当分点的数目无限增加且每个小弧段都缩向一点时，
此折线的长 $\sum_{i=1}^{n}\left|\boldsymbol{M}_{i-1} \boldsymbol{M}_{i}\right|$ 的极限存在，则称此极限为曲线弧 $\boldsymbol{A B}$ 的弧长．
设曲线弧为 $y=f(x) (a \leq x \leq b)$ ，其中 $f(x)$在 $[a, b]$ 上有一阶连续导数
弧长元素 $d s=\sqrt{1+y^{\prime 2}} d x$ 弧长 $s=\int_{a}^{b} \sqrt{1+y^{\prime 2}} d x$ ．
## 2.参数方程情形
## 3.极坐标情形
\because\left\{\begin{array}{l}
x=r(\theta) \cos \theta \\
y=r(\theta) \sin \theta
\end{array} \quad(\alpha \leq \theta \leq \beta)\right.
弧长 $s=\int_{\alpha}^{\beta} \sqrt{r^{2}(\theta)+r^{\prime 2}(\theta)} d \theta$ ．
## 计算曲线弧长习例
例3 计算曲线 $y=\frac{\mathbf{2}}{\mathbf{3}} \boldsymbol{x}^{\frac{3}{2}}$ 上相应于 $\boldsymbol{x}$ 从 $\boldsymbol{a}$ 到 $\boldsymbol{b}$ 的一段弧的长度．
例4 计算曲线 $y=\int_{0}^{\frac{x}{n}} n \sqrt{\sin } \theta d \theta$ 的弧长 $(0 \leq x \leq n \pi)$ ．
解 $\because r^{\prime}=3 a\left(\sin \frac{\theta}{3}\right)^{2} \cdot \cos \frac{\theta}{3} \cdot \frac{1}{3}=a\left(\sin \frac{\theta}{3}\right)^{2} \cdot \cos \frac{\theta}{3}$ ，
& =a \int_{0}^{3 \pi}\left(\sin \frac{\theta}{3}\right)^{2} d \theta=\frac{3}{2} \pi a
## 三、变力沿直线所作的功
## 变力作功包括有：电场力作功、气体压力作功、克服阻力作功、万有引力作功、弹力作功等。
## 变力做功习例
例11 半径为 $R$ ，比重为 $\sigma($ 大于 1$)$ 的球沉入深为 $H(>2 R)$ 的水池底，现将其从水中取出，问需作多少功？
例 8 把一个带 $+\boldsymbol{q}$ 电量的点电荷放在 $\boldsymbol{r}$ 轴上坐标原点处，它产生一个电场．这个电场对周围的电荷有作用力．由物理学知道，如果一个单位正电荷放在这个电场中距离原点为 $r$ 的地方，那么电场对它的作用力的大小为 $F=k \frac{q}{r^{2}}$（ $k$ 是常数），当这个单位正电荷在电场中从 $r=a$ 处沿 $\boldsymbol{r}$ 轴移动到 $\boldsymbol{r}=\boldsymbol{b}$ 处时，计算电场力 $\boldsymbol{F}$ 对它所作的功．
\begin{figure}
\captionsetup{labelformat=empty}
\caption{取 $r$ 为积分变量，$r \in[a, b]$ ，}
\end{figure}
取任一小区间 $[r, r+d r]$ ，功元素 $d w=\frac{k q}{r^{2}} d r$ ，所求功为 $w=\int_{a}^{b} \frac{k q}{r^{2}} d r=k q\left[-\frac{1}{r}\right]_{a}^{b}=k q\left(\frac{1}{a}-\frac{1}{b}\right)$ ．
\begin{figure}
\captionsetup{labelformat=empty}
\caption{解 建立坐标系如图}
\end{figure}
\begin{figure}
\captionsetup{labelformat=empty}
\caption{取 $x$ 为积分变量， $x \in[0,5]$}
\end{figure}
这一薄层水的重力为 $g \pi y^{2} d x \cdot 1=g \pi\left(R^{2}-x^{2}\right) d x$故 $d w=g \pi x\left(R^{2}-x^{2}\right) d x$
\therefore w=\int_{0}^{R} g \pi x\left(R^{2}-x^{2}\right) d x .
（2）如图所示建立坐标系，则边界曲线方程为 $x^{2}+y^{2}=R^{2}$选 $y$ 为积分变量，$y \in[0, R] \forall[y, y+d y] \subset[0, R]$,
这一薄层水的重力为 $g \pi y^{2} d x \cdot 1=g \pi\left[R^{2}-(x-R)^{2}\right] d x$
故 $d w=g \pi x\left[R^{2}-(x-R)^{2}\right] d x$
在水中行程为 $\boldsymbol{H}-\mathbf{2} \boldsymbol{R}+\boldsymbol{x}$ ，在水外行程为 $H-(H-2 R+x)=2 R-x$ ，
设木板对铁钉的阻力为 $f(x)=k x$ ，
d w=f(x) d x=k x d x
第一次锤击时所作的功为 $w_{1}=\int_{0}^{1} k x d x=\frac{k}{2}$ ，
设 $\boldsymbol{n}$ 次击入的总深度为 $\boldsymbol{n}$ 厘米
$n$ 次锤击所作的总功为 $w_{h}=\int_{0}^{h} f(x) d x$ ．
## 四、液体压力
小矩形片的面积为 $2 \sqrt{\boldsymbol{R}^{2}-\boldsymbol{x}^{2}} d \boldsymbol{x}$ ．
小矩形片的压力元素为 $\boldsymbol{d F}=\mathbf{2 \gamma} \boldsymbol{x} \sqrt{\boldsymbol{R}^{\mathbf{2}}-\boldsymbol{x}^{\mathbf{2}}} \boldsymbol{d} \boldsymbol{x}$端面上所受的压力
& =-\gamma\left[\frac{2}{3}\left(\sqrt{R^{2}-x^{2}}\right)^{3}\right]_{0}^{R}=\frac{2 \gamma}{3} R^{3} .
例14将直角边各为 $\boldsymbol{a}$ 及 $\mathbf{2 a}$ 的直角三角形薄板垂直地浸人水中，斜边朝下，直角边的长边与水面平行，且该边到水面的距离恰等于该边的边长，求薄板所受的侧压力。
斜边方程为 $y=2(a-x)$ ，
积分变量为 $x \in[0, a]$ ，
$\forall[x, x+d x] \subset[0, a]$ ，小面积 $2(a-x) d x$ ，
$\therefore d F=(x+2 a) \cdot 2(a-x) \cdot 1 \cdot \gamma d x$
## 五、万有引力
由物理学知道，质量分别为 $\boldsymbol{m}_{\mathbf{1}}, \boldsymbol{m}_{\mathbf{2}}$ 相距为 $r$ 的两个质点间的引力的大小为 $F=k \frac{m_{1} m_{2}}{r^{2}}$ ，其中 $k$ 为引力系数，引力的方向沿着两质点的连线方向．
## 万有引力习例
小段的质量为 $\rho d y$ ，
d F_{x}=d F \cdot \cos \alpha=-k \frac{a m \rho d y}{\left(a^{2}+y^{2}\right)^{\frac{3}{2}}},
& \therefore \vec{F}=\left\{\frac{-2 k m \rho l}{a\left(4 a^{2}+l^{2}\right)^{\frac{1}{2}}}, 0\right\}
## 六、函数的平均值与均方根
（2）求和：设各分点处的函数值为 $y_{0}, y_{1}, y_{2}, \cdots, y_{n}$函数 $f(x)$ 在区间 $[a, b]$ 止的平均值近似为
$\bar{y}=\frac{1}{b-a} \int_{a}^{b} f(x) d x \quad \longrightarrow$ 几何平均值公式
区间长度 $\quad \longrightarrow=(b-a) \bar{y}=(b-a) f(\xi)$
## 函数平均值习例
## 2.均方根
通常交流电器上标明的功率就是平均功率．交流电器上标明的电流值都是一种特定的平均值，习惯上称为有效值．
周期性非恒定电流 $i$（如正弦交流电）的有效值规定如下：当 $i(t)$ 在它的一个周期 $T$ 内在负载电阻 $\boldsymbol{R}$ 上消耗的平均功率，等于取固定值 $\boldsymbol{I}$ 的恒定电流在 $R$ 上消耗的功率时，称这个值 $I$ 为 $i(t)$ 的有效值．
\therefore I^{2}=\frac{1}{T} \int_{0}^{T} i^{2}(t) d t \quad \text { 即 } I=\sqrt{\frac{1}{T} \int_{0}^{T} i^{2}(t) d t} \text {. }
## 内容小结
