## 同理可得

b．设曲面的方程为：$x=g(y, z)$
曲面面积公式为：$A=\iint_{D_{y z}} \sqrt{1+\left(\frac{\partial x}{\partial y}\right)^{2}+\left(\frac{\partial x}{\partial z}\right)^{2}} d y d z$ ；
c．设曲面的方程为： $\boldsymbol{y}=\boldsymbol{h}(\boldsymbol{z}, \boldsymbol{x})$
曲面面积公式为 ：$A=\iint_{D_{z x}} \sqrt{1+\left(\frac{\partial y}{\partial z}\right)^{2}+\left(\frac{\partial y}{\partial x}\right)^{2}} d z d x$ 。
注意：求曲面的面积时，只需先选择好曲面向哪个坐标面投影，再画出曲面的投影图即可。

例5 求半径为 $a$ 的球的表面积．

例6 求由曲面 $z=x y$ 被 $\sqrt{x^{2}+y^{2}}=15$ 所截部分的面积．

例7 求球面 $x^{2}+y^{2}+z^{2}=a^{2}$ ，含在圆柱体 $x^{2}+y^{2}=a x$ 内部的那部分面积．

