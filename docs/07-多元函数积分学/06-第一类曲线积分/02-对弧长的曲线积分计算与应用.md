## 1.直接计算法

定理1．（1）设 $f(x, y)$ 在 $L$ 上连续，
（2）$L$ 的参数方程为 $\left\{\begin{array}{l}x=\varphi(t), \\ y=\psi(t),\end{array}(\alpha \leq t \leq \beta)\right.$,
（3）$\varphi(t), \psi(t)$ 在 $[\alpha, \beta]$ 上具有一阶连续导数，且 $\varphi^{\prime 2}(t)+\psi^{\prime 2}(t) \neq 0$,

则 $\int_{L} f(x, y) d s=\int_{\alpha}^{\beta} f[\varphi(t), \psi(t)] \sqrt{\varphi^{\prime 2}(t)+\psi^{\prime 2}(t)} d t$

$$
(\alpha<\beta)
$$

注意：（1）积分下限 $\alpha$ 一定小于上限 $\beta$ ；

$$
\because \Delta s_{i}>0, \therefore \Delta t_{i}>0 .
$$

（2）计算时，将 $x, y, d s$ 换成 $\varphi(t), \psi(t), \sqrt{\varphi^{\prime 2}}(t)+\psi^{\prime 2}(t) d t$ ，再作 $\alpha \rightarrow \beta$ 的积分．
概括为＂一代二换三定限＂

$$
\begin{aligned}
& L: y=\psi(x) \quad(a \leq x \leq b) \\
& \int_{L} f(x, y) d s=\int_{a}^{b} f[x, \psi(x)] \sqrt{1+\psi^{\prime 2}(x)} d x
\end{aligned}
$$

$$
\begin{aligned}
& L: x=\varphi(y) \quad(c \leq y \leq d) \\
& \int_{L} f(x, y) d s=\int_{c}^{d} f[\varphi(y), y] \sqrt{1+\varphi^{\prime 2}(y)} d y
\end{aligned}
$$

(5) $L: r=r(\theta), \quad(\alpha \leq \theta \leq \beta)$.

$$
\begin{aligned}
& \Rightarrow L:\left\{\begin{array}{l}
x=r(\theta) \cos \theta \\
y=r(\theta) \sin \theta
\end{array}, \quad(\alpha \leq \theta \leq \beta)\right. \\
& \int_{L} f(x, y) d s=\int_{\alpha}^{\beta} f(r \cos \theta, r \sin \theta) \sqrt{r^{2}(\theta)+r^{\prime 2}(\theta)} d \theta
\end{aligned}
$$

(6) $\Gamma:\left\{\begin{array}{l}x=\varphi(t) \\ y=\psi(t), \quad(\alpha \leq t \leq \beta) . \\ z=\omega(t)\end{array}\right.$

$$
\begin{aligned}
& \int_{\Gamma} f(x, y, z) d s \\
& =\int_{\alpha}^{\beta} f[\varphi(t), \psi(t), \omega(t)] \sqrt{\varphi^{\prime 2}(t)+\psi^{\prime 2}(t)+\omega^{\prime 2}(t)} d t
\end{aligned}
$$

例1．计算 $\oint_{L}(x+y) d s$ ，其中 $L$ 是以 $O(0,0)$ ， $A(1,0), B(0,1)$ 为顶点的三角形的边．

例2。计算 $\oint_{L} e^{\sqrt{x^{2}+y^{2}}} d s$ ，其中 $L$ 为圆周 $x^{2}+y^{2}=a^{2}$ ，直线 $y=x$ 及 $o x$ 轴在第一象限内所围成的扇形的整个边界。

例3。计算 $\int_{\Gamma} x^{2} y z d s$ ，其中 $\Gamma$ 为折线 $A(0,0,0)$ ，

$$
B(0,0,2), C(1,0,2), D(1,3,2) .
$$

例1．计算 $\oint_{L}(x+y) d s$ ，其中 $L$ 是以 $O(0,0), A(1,0), B(0,1)$为顶点的三角形的边。
解

$$
\begin{aligned}
& \overline{O A}: y=0, \quad 0 \leq x \leq 1 ; \\
& \overline{A B}: y=1-x, \quad 0 \leq x \leq 1 ; \\
& \overline{B O}: x=0, \quad 0 \leq y \leq 1 ; \\
& \begin{aligned}
\therefore \int_{L}(x+y) d s & =\int_{O A}+\int_{A B}+\int_{B O} \\
\quad= & \int_{0}^{1} x d x+\int_{0}^{1} \sqrt{2} d x+\int_{0}^{1} y d y=1+\sqrt{2} .
\end{aligned}
\end{aligned}
$$

例2。计算 $\oint_{L} e^{\sqrt{x^{2}+y^{2}}} d s$ ，其中 $L$ 为圆周 $x^{2}+y^{2}=a^{2}$ ，直线 $y=x$ 及 $o x$ 轴在第一象限内所围成的扇形的整个边界。解

$$
\overline{O A}: y=0, \quad 0 \leq x \leq a ;
$$

$$
\begin{aligned}
& A B:\left\{\begin{array}{l}
x=a \cos t \\
y=a \sin t
\end{array}, \quad 0 \leq t \leq \frac{\pi}{4} ;\right. \\
& \overline{B O}: y=x, \quad 0 \leq x \leq \frac{a}{\sqrt{2}} ; \\
& \therefore \int_{L} e^{\sqrt{x^{2}+y^{2}}} d s=\int_{O A}+\int_{A B}+\int_{B O} \\
& =\int_{0}^{a} e^{x} d x+\int_{0}^{\frac{\pi}{4}} e^{a} a d t+\int_{0}^{\frac{a}{\sqrt{2}}} e^{\sqrt{2} x} \sqrt{2} d x=e^{a}\left(2+\frac{\pi}{4} a\right)-2 .
\end{aligned}
$$

例3。计算 $\int_{\Gamma} x^{2} y z d s$ ，其中 $\Gamma$ 为折线 $A(0,0,0), B(0,0,2)$ ， $C(1,0,2), D(1,3,2)$.
解

$$
\begin{aligned}
& \overline{A B}:\left\{\begin{array}{l}
x=0 \\
y=0
\end{array}, \quad 0 \leq z \leq 2 ; \quad \overline{B C}:\left\{\begin{array}{l}
y=0 \\
z=2
\end{array}, \quad 0 \leq x \leq 1 ;\right.\right. \\
& \overline{C D}: \begin{cases}x=1 & 0 \leq y \leq 3 ; \\
z=2 & , \quad 0 \leq y \leq 1\end{cases} \\
& \therefore \int_{\Gamma} x^{2} y z d s=\int_{A B}+\int_{B C}+\int_{C D}=0+0+\int_{\frac{C D}{C D}} x^{2} y z d s \\
& \quad=\int_{0}^{3} 2 y d y=\left.y^{2}\right|_{0} ^{3}=9 .
\end{aligned}
$$

## 2.利用对称性简化计算

（1）当 $L$ 对称于 $x$ 轴时，

$$
\int_{L} f(x, y) d s= \begin{cases}2 \int_{L_{\text {上 }}} f(x, y) d s & f(x,-y)=f(x, y) \\ 0 & f(x,-y)=-f(x, y)\end{cases}
$$

（2）当 $L$ 对称于 $y$ 轴时，

$$
\int_{L} f(x, y) d s= \begin{cases}2 \int_{L_{\text {右 }}} f(x, y) d s & f(-x, y)=f(x, y) \\ 0 & f(-x, y)=-f(x, y)\end{cases}
$$

例4．求 $I=\int_{L} y d s$ ，其中 $L: y^{2}=4 x$ ，从 $(1,2)$ 到 $(1,-2)$ 一段．

例5．计算 $I=\int_{L}|y| d s$ ，其中 $L$ 为双纽线 $\left(x^{2}+y^{2}\right)^{2}=a^{2}\left(x^{2}-y^{2}\right)$ 的弧。

例6．求 $I=\int_{\Gamma} x^{2} d s$ ，其中 $\Gamma$ 为圆周 $\left\{\begin{array}{l}x^{2}+y^{2}+z^{2}=a^{2} \\ x+y+z=0\end{array}\right.$ ．

例4．求 $I=\int_{L} y d s$ ，其中 $L: y^{2}=4 x$ ，从 $(1,2)$ 到 $(1,-2)$ 一段．
方法1．由于 $L$ 关于 $x$ 轴对称，被积函数是关于 $\boldsymbol{y}$ 的奇函数．

$$
\therefore I=\int_{L} y d s=0 .
$$

方法2．选取 $x=\frac{y^{2}}{4},(-2 \leq y \leq 2)$ ．

$$
I=\int_{-2}^{2} y \sqrt{1+\left(\frac{y}{2}\right)^{2}} d y=0 .
$$

例5．计算 $I=\int_{L}|y| d s$ ，其中 $L$ 为双纽线 $\left(x^{2}+y^{2}\right)^{2}=a^{2}\left(x^{2}-y^{2}\right)$ 的弧。
解 双纽线的极坐标方程为 $r^{2}=a^{2} \cos 2 \theta$ ，

$$
\begin{aligned}
& \left\{\begin{array}{l}
x=a \sqrt{\cos 2 \theta} \cos \theta \\
y=a \sqrt{\cos 2 \theta} \sin \theta
\end{array}\right. \\
& \quad \text { 且 } d s=\sqrt{r^{2}+r^{\prime 2}} d \theta=\frac{a}{\sqrt{\cos 2 \theta}} d \theta
\end{aligned}
$$

于是 $I=\int_{L}|y| d s=4 \int_{L_{1}}|y| d s$

$$
\begin{aligned}
& =4 \int_{0}^{\frac{\pi}{4}} a \sqrt{\cos 2 \theta} \sin \theta \frac{a}{\sqrt{\cos 2 \theta}} d \theta \\
& =4 a^{2} \int_{0}^{\frac{\pi}{4}} \sin \theta d \theta=2(2-\sqrt{2}) a^{2} .
\end{aligned}
$$

例6．求 $I=\int_{\Gamma} x^{2} d s$ ，其中 $\Gamma$ 为圆周 $\left\{\begin{array}{l}x^{2}+y^{2}+z^{2}=a^{2} \\ x+y+z=0\end{array}\right.$ ．
解法1．由 $\left\{\begin{array}{l}x^{2}+y^{2}+z^{2}=a^{2} \\ x+y+z=0\end{array}\right.$ 消去 $z$ 得

$$
\begin{aligned}
& 2 x^{2}+2 x y+2 y^{2}=a^{2} \\
& \left(x+\frac{y}{2}\right)^{2}+\left(\frac{\sqrt{3} y}{2}\right)^{2}=\left(\frac{a}{\sqrt{2}}\right)^{2}
\end{aligned}
$$

令 $x+\frac{y}{2}=\frac{a}{\sqrt{2}} \cos t, \frac{\sqrt{3} y}{2}=\frac{a}{\sqrt{2}} \sin t$

$$
\begin{aligned}
& \text { 得 }\left\{\begin{array}{l}
x=\frac{a}{\sqrt{2}} \cos t-\frac{a}{\sqrt{6}} \sin t \\
y=\frac{a}{\sqrt{6}} \sin t \\
z=-\frac{a}{\sqrt{2}} \cos t-\frac{a}{\sqrt{6}} \sin t
\end{array} \quad(0 \leq t \leq 2 \pi)\right. \\
& \therefore I=\int_{\Gamma} x^{2} d s=a \int_{0}^{2 \pi}\left(\frac{a}{\sqrt{2}} \cos t-\frac{a}{\sqrt{6}} \sin t\right)^{2} d t \\
& \quad=a \int_{0}^{2 \pi}\left(\frac{a^{2}}{2} \cos ^{2} t-\frac{a^{2}}{\sqrt{12}} \cos t \sin t+\frac{a^{2}}{6} \sin ^{2} t\right) d t=\frac{2 \pi a^{3}}{3} .
\end{aligned}
$$

解法2 由轮换对称性，知

$$
\int_{\Gamma} x^{2} d s=\int_{\Gamma} y^{2} d s=\int_{\Gamma} z^{2} d s .
$$

故 $I=\frac{1}{3} \int_{\Gamma}\left(x^{2}+y^{2}+z^{2}\right) d s=\frac{a^{2}}{3} \int_{\Gamma} d s=\frac{2 \pi a^{3}}{3}$ ．
（ $\int_{\Gamma} d s=2 \pi a$ ，球面大圆周长）
这里巧妙地利用了轮换对称性，使计算大大简化。一般来讲，对曲线的方程，若其各坐标的＂地位＂完全平等，则可考虑利用轮换对称性。

另外，若被积函数出现积分曲线方程的形式，则通常可以代入将积分化简。

## 四.对弧长的曲线积分的应用

（1）当 $\rho(x, y)$ 表示 $L$ 的线密度时，$m=\int_{L} \rho(x, y) d s$ ；
（2）当 $f(x, y) \equiv 1$ 时，$L_{\text {弧长 }}=\int_{L} d s$ ；
（3）当 $f(x, y)$ 表示立于 $L$ 上的柱面在点 $(x, y)$ 处的高时，

$$
S_{\text {柱面面积 }}=\int_{L} f(x, y) d s \text {. }
$$

（4）曲线弧的重心坐标 $x=\frac{\int_{L} x \rho d s}{\int_{L} \rho d s}, \quad \bar{y}=\frac{\int_{L} y \rho d s}{\int_{L} \rho d s}$ ．
（5）曲线弧的转动惯量

$$
I_{x}=\int_{L} y^{2} \rho d s, I_{y}=\int_{L} x^{2} \rho d s, I_{o}=\int_{L}\left(x^{2}+y^{2}\right) \rho d s .
$$

例7．设有曲线形构件方程为 $x=a \cos t, y=a \sin t, z=k t$ ，解 $(0 \leq t \leq 2 \pi), \rho(x, y, z)=x^{2}+y^{2}+z^{2}$ ，求 $I_{z},(\bar{x}, \bar{y}, \bar{z})$ ．
（1）$I_{z}=\int_{\Gamma}\left(x^{2}+y^{2}\right) \rho(x, y, z) d s=\int_{\Gamma}\left(x^{2}+y^{2}\right)\left(x^{2}+y^{2}+z^{2}\right) d s$

$$
\begin{aligned}
& =\int_{0}^{2 \pi} a^{2}\left(a^{2}+k^{2} t^{2}\right) \sqrt{a^{2}+k^{2}} d t \\
& =\frac{2}{3} \pi a^{2} \sqrt{a^{2}+k^{2}}\left(3 a^{2}+4 \pi^{2} k^{2}\right) .
\end{aligned}
$$

（2）$m=\int_{\Gamma} \rho(x, y, z) d s=\int_{\Gamma}\left(x^{2}+y^{2}+z^{2}\right) d s$

$$
=\int_{0}^{2 \pi}\left(a^{2}+k^{2} t^{2}\right) \sqrt{a^{2}+k^{2}} d t=\frac{2}{3} \pi \sqrt{a^{2}+k^{2}}\left(3 a^{2}+4 \pi^{2} k^{2}\right) .
$$

$$
\begin{aligned}
& \int_{\Gamma} x\left(x^{2}+y^{2}+z^{2}\right) d s=\int_{0}^{2 \pi} a \cos t\left(a^{2}+k^{2} t^{2}\right) \sqrt{a^{2}+k^{2}} d t \\
& =a k^{2} \sqrt{a^{2}+k^{2}} \int_{0}^{2 \pi} t^{2} \cos t d t=4 a k^{2} \sqrt{a^{2}+k^{2}} \pi \\
& \begin{aligned}
\int_{\Gamma} y\left(x^{2}+y^{2}+z^{2}\right) d s & =\int_{0}^{2 \pi} a \sin t\left(a^{2}+k^{2} t^{2}\right) \sqrt{a^{2}+k^{2}} d t \\
& =-4 a k^{2} \sqrt{a^{2}+k^{2}} \pi^{2}
\end{aligned} \\
& \begin{aligned}
\int_{\Gamma} z\left(x^{2}+y^{2}+z^{2}\right) d s & =\int_{0}^{2 \pi} k t\left(a^{2}+k^{2} t^{2}\right) \sqrt{a^{2}+k^{2}} d t \\
& =2 k \pi\left(\pi a^{2}+2 \pi^{3} k^{2}\right)
\end{aligned} \\
& \begin{aligned}
\therefore x=6 a k^{2} /\left(3 a^{2}+4 \pi^{2} k^{2}\right), \bar{y}=-6 \pi a k^{2} /\left(3 a^{2}+4 \pi^{2} k^{2}\right), \\
\bar{z}=3 k\left(\pi a^{2}+2 \pi^{3} k^{2}\right) /\left(3 a^{2}+4 \pi^{2} k^{2}\right) .
\end{aligned}
\end{aligned}
$$
